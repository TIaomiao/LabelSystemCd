# CMR Workstation v2026.08.13-2

## 交付范围

- Series Overview 在没有人工主标记时，将排序后的第一条 `lge_sax` 明确显示为当前有效 `Tissue LGE ✓`。
- 人工切换主 LGE 后，勾选唯一移动到新序列，并在刷新或重新打开同一 Study 后保持。
- 非 `lge_sax` 不显示 Tissue 选择；Overview 不显示 Slice / Phase Matrix。
- 普通“重新打开”复用既有 Study；受控重导入按 Series UID 恢复人工角色和 Tissue 主标记。
- LGE 阈值预览和重算携带当前 phase，补齐 MVO / Grey Zone 边界回归。

## 验证

- `apps/web` typecheck、13 项测试、production bundle 通过。
- 主站 TypeScript/Vite production build 通过。
- CVI API 定向回归 22 项通过；病例访问保护 20 项通过。
- 真实浏览器验证 Study 735：默认第一条主 LGE、切换到第二条、刷新后保持第二条，Matrix 可见数为 0。

## 发布与回滚

- 发布：`scripts/release_workstation.sh v2026.08.13-2`
- 回滚：`scripts/rollback_workstation.sh <旧版本 tag>`，生成新的回滚 commit，不重写 Git 历史。
- 脚本仅处理受 Git 管理的源码，不包含数据库、病例/DICOM、日志、密钥、`/home/Larry/data/` 或忽略的运行目录。
