# v2026.07.23-1 实际上线记录

上线日期：2026-07-23（Asia/Shanghai）

技术结果：成功

执行范围：LabelSystem 数据集整库权限管理、权限审计、后台面板和杨楠 `CMR_SCS` 主库整库授权。

执行身份：项目负责人在本次会话明确批准上线；本机通过已审核管理员账号 `lzq` 调用正式权限接口。技术核验由 Codex 完成，尚未登记独立人工复核人。

## 1. 发布源码身份

- 基础 Git commit：`bffad1c2652d723e754a0ca03fa3241286be0343`。
- 当前分支：`main`。
- 本次功能位于保留既有用户改动的脏工作区中；基础 commit 不包含完整发布功能，不能单独作为发布身份。
- 规范化运行源码快照 SHA-256：`2ac4f8a96b8faa0089598632182cb43352203c5c7ec2772bd97a796cf6dca436`。
- 受控源码包：`/home/Larry/backups/LabelSystem/v2026.07.23-1/labelsystem-source-v2026.07.23-1-20260723T152926+0800.tar.gz`。
- 源码包 SHA-256：`e1cd08a4d6bf7faab53ba6789009a730ded9ab24af236f6ca47912efb82edd3b`；权限 `600`；`gzip -t` 通过。
- 关键文件和前端构建产物校验和见[源码与构建清单](v2026.07.23-1_source_manifest.md)。

本次未创建 Git commit 或 tag。恢复发布源码时必须同时使用受控源码包及上述 SHA-256，不能只回退到基础 commit。

## 2. 上线前门禁和备份

- 数据集权限专项：18/18 通过。
- 完整后端：149/149 通过。
- 前端 TypeScript/Vite 生产构建通过。
- 后端 `py_compile`、`git diff --check` 通过。
- 生产数据库上线前 `PRAGMA integrity_check` 为 `ok`。
- 上线前数据库备份：`/home/Larry/backups/LabelSystem/v2026.07.23-1/labelsystem-predeploy-20260723T152926+0800.sqlite3`。
- 数据库备份 SHA-256：`95ded3821bc01964b698f35ca236125b9be7edceca242bfda3099e21eb2132a9`；权限 `600`；备份完整性为 `ok`。

备份路径位于仓库之外，未把数据库、患者内容、凭据、Cookie 或完整日志写入发布文档。

## 3. 服务切换和日志

- 15:33:37 轮转旧 `backend.log` 和 `frontend.log`，随后仅对 LabelSystem Flask 与 Vite 执行第一次精确切换。
- 第一次切换后，本地后端 `5000` 和前端 `5173` 均返回 200；新增表和触发器已创建。
- 15:39:52 观察到另一个 Larry 远程会话重新拉起整套现有服务，CVI API、Showcase 和 FRP 的 PID 因此外部切换而变化。该第二次切换不是本版本所需操作；发现后已按最终运行进程重新完成健康、权限和隔离核验。
- 最终监听：Flask `127.0.0.1:5000`、Vite `127.0.0.1:5173`、CVI API `127.0.0.1:8010`、Showcase `3015`；FRP 正常运行。
- 当前与轮转后的 LabelSystem 前后端日志权限均已设为 `600`。
- 最终观察窗口内，LabelSystem 前后端日志未匹配到 traceback、exception、internal server error 或启动失败。
- 本地前后端入口和公网 `http://47.108.84.221:8282/` 均返回 200。

## 4. 数据库和版本核验

- `dataset_access_grant`、`dataset_access_audit` 两张表存在。
- `dataset_access_audit_no_update`、`dataset_access_audit_no_delete` 两个 SQLite 触发器存在。
- 在事务内尝试更新和删除审计行均被触发器拒绝，回滚后审计总数不变。
- 上线后数据库 `PRAGMA integrity_check` 为 `ok`。
- 管理员接口返回权限模块版本 `v2026.07.23-1`，孤立有效授权为 0。

版本口径：`v2026.07.23-1` 是本次 LabelSystem 权限模块发布版本；工作站顶部 `v2026.07.16-4` 是临床工作站功能版本。本次没有变更曲率或其他临床测量算法，因此没有改动工作站功能版本号。

## 5. 批准权限变更

- 目标账号：杨楠，user_id `41`，已审核普通账号。
- `CMR_SCS` 原有病例任务：482；授权后仍为 482，没有删除、替换或复制病例任务。
- 正式接口读取最新 revision 后，保留原整库授权名单并新增杨楠。
- 杨楠当前 `CMR_SCS` 整库有效授权为 1，页面语义为“整库权限”，未来新增病例自动包含。
- `CMR_SCS_2`、`CMR_SCS_PAH_TEST`、`CMR_SCS_PAH_1` 对杨楠的整库有效授权均为 0。
- 本次实际变化产生且仅产生 1 条 `grant` 审计；批次 ID：`de96ccf8-b4a8-47cf-a216-dcf4cb49ced8`。
- 审计快照为管理员 `lzq`、目标杨楠、真实数据集 `CMR_SCS`、版本 `v2026.07.23-1`、本机请求地址 `127.0.0.1`。
- 使用最新 revision 重复保存相同名单产生 0 条新审计；生产审计总数保持为 1。

## 6. 最终线上验证

- 管理员读取 `/api/admin/dataset-access`：200。
- 杨楠读取管理员数据集权限接口：403。
- 杨楠读取 `CMR_SCS` 整库病例源：200；目录总数为 482，来源显示为 `dataset_grant`。
- 杨楠对 `CMR_SCS_2`、`CMR_SCS_PAH_TEST`、`CMR_SCS_PAH_1` 的病例导入请求：均为 403，未触发导入。
- 两个已导入 PAH Study 的直接对象访问：均为 403。
- 公网首页和公网鉴权状态接口：均为 200。
- 最终服务切换后再次核对授权、审计、隔离和数据库完整性，结果不变。

## 7. 回滚锚点和剩余事项

- 代码回滚优先使用受控源码包；数据库通常不需要恢复，新增审计必须保留。
- 只有业务负责人和数据库负责人明确批准并接受上线后写入丢失时，才允许恢复上线前数据库备份。
- 当前进程沿用现网 `setsid` 启动方式，并非 systemd/Supervisor 托管；后续可单独建设服务守护和自动启动，不影响本次功能已上线的结论。
- 独立人工复核和医生业务验收尚未在本记录中签字；技术上线与权限隔离验证已完成。
