# 工作日志

## 2026-06-07

### 已确认事实

- 阅读了 `README_RESTART.md` 的前 90 行，只读确认它描述的是 `backend/ + frontend/` 启动方式。
- 用户在终端直接输入 `label_sys` 后出现 `command not found`。
- 只读执行 `conda env list`，确认服务器存在 Conda 环境 `label_sys`。
- 只读确认 `label_sys` 环境里的工具版本：
  - Python 3.9.25
  - Node.js v22.22.1
  - npm 10.9.4
  - Flask 3.1.2
- 在 `zian_workspace/restart_execution_guide.md` 写入个人执行指南。
- 用户执行 `conda run -n label_sys python backend/app.py` 后，后端在导入 `backend/routes.py` 时失败。
- 报错位置是 `backend/routes.py` 的 `_dataset_aliases(dataset: str | None) -> list[str]`。
- `label_sys` 的 Python 版本是 3.9.25；当前 base Python 版本是 3.12.2。
- 只读扫到后端多个文件包含 `str | None`、`dict[...]`、`list[...]` 等较新的类型注解写法。
- 不启动服务的前提下，base Python 可以导入 Flask、pydicom、PIL、numpy、pandas、cv2、torch、httpx、openai、flask_login。
- 用户执行 `conda activate label_sys` 后，终端提示符变为 `(label_sys)`，说明激活环境成功。
- 用户在 `(label_sys)` 下执行 `python backend/app.py` 后仍然失败；该现象先记录为“手动重复启动时的报错”，不直接作为环境损坏结论。
- 尝试只读探查进程/端口时，Codex 沙箱无法可靠看到真实主机上的服务监听状态。
- 师兄反馈：环境肯定没问题，运行不了更可能是因为后端进程本来就在运行。
- 进一步确认：`127.0.0.1:5000` 有 Werkzeug 响应，响应头显示 `Python/3.12.2`。
- 进一步确认：监听 5000 的后端进程是 PID `111100`，命令是 `/home/Larry/miniconda3/bin/python backend/app.py`，实际解释器是 `/home/Larry/miniconda3/bin/python3.12`。
- 进一步确认：`127.0.0.1:5173` 返回 `200 OK`，前端 Vite 进程也在运行。

### 推测

- `label_sys` 环境按师兄反馈先视为可用，不要因为一次手动启动失败就急着改环境。
- 当前接手时应优先理解 `README_RESTART.md` 对应的 `backend/ + frontend/` 路线，再对照根目录 `README.md` 里的 `apps/` 路线。
- 当前最重要的不是纠结 Python 3.9 / 3.10，而是先确认已有后端服务是否正在跑、应该访问哪个地址。
- 师兄说“后端现在应该在运行”时，是在提醒先检查现有服务，而不是让用户重复启动一个后端实例。
- 当前真实运行后端使用的是 base/miniconda Python 3.12；`label_sys` 的 Python 3.9 不代表当前线上/现有后端进程的解释器。

### 下一步建议

- 先读 `backend/app.py`，理解 Flask 后端从哪里启动。
- 再读 `frontend/package.json` 和 `frontend/src/main.tsx`，理解前端从哪里启动。
- 暂时不要安装依赖、迁移数据库、重启服务或提交 Git。
- 启动后端前，先向负责人确认当前服务访问地址，以及是否已经有后端进程在跑。
- 如果只是确认服务是否已运行，优先用浏览器访问或 `curl -I` 检查端口，不要直接再次启动。
- 当前可先访问 `http://127.0.0.1:5000` 和 `http://127.0.0.1:5173` 做本机服务确认；`404` 或 `401` 只要来自 Werkzeug，也说明后端活着。

### 接手规划与工作日志同步

#### 已确认事实

- 用户已经完成 GitHub 工程备份。
- 用户提出希望把“读取 Notion / 写入工作日志”的动作纳入个人接手规划。
- 当前会话没有暴露可用的 Notion 写入工具。
- 已创建 `zian_workspace/AGENTS.md` 作为个人工作区规则文件。
- `zian_workspace/AGENTS.md` 中已加入规则：当 Notion 工具可用时优先写入 Notion；Notion 工具不可用时，先写入 `zian_workspace/work_log.md` 暂存。
- `zian_workspace/AGENTS.md` 中已加入输出识别规则：每次回复称呼用户为“宝贝”，并区分“已确认事实”和“推测”。

#### 推测

- 用户当前最大的卡点不是某一段代码，而是不知道每天应该形成什么产出。
- 把“项目只读验证 -> 工作日志 -> 问题清单 -> 下一步计划”固定下来，可以明显降低接手焦虑。

#### 下一步建议

- 后续每次学习项目时，先生成一条工作日志。
- 每条工作日志固定包含：今日目标、已确认事实、推测、看过的路径、风险边界、当前问题、下一步。
- 如果 Notion 工具在新会话中可用，就把本地 `work_log.md` 的新增内容同步到 Notion。

### 个人规则边界澄清

#### 已确认事实

- 当前会话没有暴露 Notion 写入工具或 Notion MCP 资源。
- 已更新 `zian_workspace/AGENTS.md`，加入“对话作用域”规则。
- 已更新 `zian_workspace/AGENTS.md`，加入“新对话启动提示”。
- 已更新 `zian_workspace/AGENTS.md`，明确当前总目标是安全、系统地了解整个 CMR AI 平台。
- 已更新 `zian_workspace/AGENTS.md`，明确 GitHub 私有工程备份是已完成的阶段性任务，不再作为当前长期目标反复展开。

#### 推测

- 用户担心个人学习对话的偏好污染上游协作者语境，因此需要把 zian 个人工作区规则和项目公共规则隔离。
- 后续新对话只要开头明确引用 `zian_workspace/AGENTS.md`，就能降低 Codex 把项目根目录规则、师兄语境和 zian 个人学习语境混在一起的风险。

#### 下一步建议

- 新对话开头使用 `zian_workspace/AGENTS.md` 中的“新对话启动提示”。
- 近期工作重点放在项目地图、运行地图、功能地图和风险边界，不再默认回到 GitHub 备份话题。

### Notion 连接复查

#### 已确认事实

- 用户询问当前会话是否已经可以写入 Notion。
- 重新搜索当前会话可用工具后，仍未发现 Notion page/database/write/append/update 相关工具。
- 因当前会话缺少 Notion 写入能力，Codex 无法直接把工作日志同步到 Notion。

#### 推测

- 另一个对话里可能已经完成了 Notion 配置，但该配置没有暴露到当前这个 Codex 会话。
- 需要在带有 Notion 工具的同一个会话里执行同步，或重新启动/刷新当前会话后再确认工具是否可用。

#### 下一步建议

- 在已经连上 Notion 的对话里，直接要求同步 `zian_workspace/work_log.md` 的新增内容。
- 如果希望当前会话同步 Notion，需要先确认当前会话能搜索到 Notion 写入工具。

## 2026-06-09

### CMR 工作站标注功能初步体验与 QA 记录

#### 已确认事实

- 本次会话仍按 `zian_workspace/AGENTS.md` 作为 zian 个人接手/学习规则。
- 当前会话未暴露 Notion 写入工具，无法直接写入 Notion；本条日志先暂存到 `zian_workspace/work_log.md`。
- 师兄本周新安排：配合 Codex 亲自体验和 review 标注功能，重点包括不同标注方式、AI/传播标注 bug、医生标注结果是否保存好、是否有 bug，不能纯靠 Codex。
- 已通过 UI 初步理解工作站数据结构：Study 下包含多个 Series；Series 下按 Slice / Phase 组织图像；标注结果以 contour/轮廓形式显示在图像和 Slice / Phase Matrix 中。
- 已观察到 SAX cine 通常是多层多相，例如 15 层 × 27/28 相；4CH cine 通常层数少但相位多；LGE 通常多层单相。
- MPR 入口当前点击后提示“MPR 视图还在实现中。现在先保留入口，避免点击后看起来没有反馈。”，因此暂不作为本周标注 QA 主线。
- Slice / Phase Matrix 可以显示哪些格子已有轮廓，并通过“手 / AI / 传”等标记区分来源。
- UI hover 轮廓时可以看到来源、创建者、更新时间等信息。
- 代码只读确认：轮廓保存时会记录 `annotation_meta` / `frame_meta`，保存逻辑在 `apps/api/services/inference.py`，传播逻辑在 `apps/api/services/propagation_runner.py`。
- 已确认“传播”不是重新训练模型，而是用已有种子轮廓，通过图像配准/光流/形变等方式补到相邻相位或层面。
- 病例002中误触了 `传播与AI` 的 `传播补全`，任务完成后 SAX 矩阵出现大量“传”标记。
- 病例002刷新页面后，传播结果没有丢失，说明传播结果已被保存并可重新加载。
- 病例002传播后科研指标出现异常：EDV 99.53 ml、ESV 109.65 ml、SV -10.12 ml、EF -10.17%。
- 已理解 EDV 是舒张末期容积，ESV 是收缩末期容积；正常逻辑下 EDV 应大于 ESV，负 SV/负 EF 是工程上明显异常。
- 已观察到病例001与病例078虽然都显示“已标注1”，但矩阵覆盖度差异很大。
- 已明确“已标注1”不等于 SAX 每张图都已手工标注，也不等于轮廓完整度达标；它更像是模块级记录数或完成模块数。
- 病例001中大量格子显示“手工”且创建/更新时间集中在同一分钟，这可能来自批量导入或统一保存，不应直接断言为医生逐张手画。

#### 推测

- 病例002传播后出现负 EF，可能由 ED/ES 相位识别错误、传播轮廓质量差、原始种子轮廓异常或指标计算缺少防御共同导致。
- `已标注1` 当前容易误导接手者，因为它不能反映 SAX/4CH/LGE 的轮廓覆盖率、来源比例或医生完成度。
- 传播补全一旦点击就会批量写入大量轮廓，若缺少确认弹窗、预览或测试病例隔离，属于误操作风险。
- 病例001大量“手工”标记同一分钟生成，可能是历史标注批量导入、AI/传播后统一保存、或医生结果一次性提交；需要师兄确认真实含义。
- zian 不需要判断医学准确性，但可以判断工程层面的异常：刷新是否丢失、来源标记是否一致、负 EF、矩阵状态是否可信、是否串病例、是否误覆盖医生结果。

#### 可向师兄汇报

- 我已开始按真实 UI 体验标注功能，不只是读代码。
- 已理解并验证 Slice / Phase Matrix 能显示轮廓覆盖和来源标记，例如“手 / AI / 传”。
- 我误触了病例002的传播补全，系统已将传播结果保存；刷新后结果未丢失。
- 病例002传播后科研指标出现 EDV < ESV、SV/EF 为负，建议作为传播后指标异常或 ED/ES 识别异常进行复核。
- 当前 `已标注1` 不能代表轮廓完整度；病例001和病例078都显示 `已标注1`，但矩阵覆盖度明显不同。
- 病例001大量“手工”标记在同一分钟生成，需确认这是医生真实手工标注、历史批量导入，还是统一保存导致的元数据现象。
- 传播补全是高影响操作，可能需要测试病例隔离、二次确认、预览或回滚机制，避免误改医生正式结果。

#### 需要向师兄确认的问题

- 哪些病例是允许我测试保存、传播、AI 分割的安全病例？
- 病例002被我误触传播补全后，是否需要回滚？如果需要，应该由谁回滚、按什么流程回滚？
- 本周验收的“标注完成”标准是什么：看模块级 `已标注1`，还是看 SAX/4CH/LGE 的轮廓覆盖率？
- SAX 心功能标注最低要求是什么：只要 ED/ES 关键相位，还是要全相位全层传播补全？
- 4CH 是否也需要系统性标注？如果需要，哪些 ROI 是必须项？
- LGE 是否要求所有层都有 endo/epi/remote/enhanced/MVO/exclude，还是允许部分层为空？
- `手工` 标记的真实含义是什么：医生逐张手画、人工修订、历史导入，还是系统统一保存后的来源？
- 负 EF 是否可作为明确 bug 记录？师兄希望归类为 ED/ES 识别问题、传播质量问题，还是指标计算防御问题？
- 当前是否有查看轮廓来源比例的后台接口或页面，例如手工/AI/传播各覆盖多少 frame？
- 是否需要给传播补全、清空全部轮廓等高风险操作增加二次确认或只在测试病例开放？

#### 下一步

- 停止在正式病例上继续传播或清空轮廓，先等待师兄确认测试范围。
- 继续用只读/低风险方式记录不同病例的矩阵覆盖情况：总格子数、有轮廓格子数、手工格子数、AI 格子数、传播格子数。
- 形成一个 QA 表格字段：病例、模块、Series、slice/phase、操作、预期、实际、来源标记、刷新后是否保留、指标是否异常、截图。
- 优先验证三个高价值点：保存刷新是否丢、传播是否误覆盖/负 EF、`已标注1` 是否误导完成度判断。

## 2026-06-10

### 根据师兄反馈调整本周标注 QA 重心

#### 已确认事实

- 用户已向师兄汇报病例测试范围、病例002误触传播补全、标注完成标准、传播后科研指标异常/变化、`已标注1` 与实际覆盖度不一致、病例001手工标注时间集中等问题。
- 师兄反馈：`传播与AI` 里可能有 bug，但需要分阶段检查。
- 师兄反馈：当前第一阶段优先检查手工标注问题，尤其是勾画面板。
- 师兄反馈：历史上可能存在“已经标注但取消不了”等问题。
- 师兄反馈：`显示标注` 可能有问题；正常情况下管理员应能看到医生的标注。
- 师兄反馈：一般医生只会标注 1-2 帧，其余依赖 AI 传播。
- 师兄反馈：目前让医生主要标注 4CH 序列。
- 师兄反馈：SAX 序列中 AI 目前能够全自动分割前 3 个部位，因此不需要医生标注；最后一个部位“脂肪”需要医生标注。
- 只读检查前端维护记录时看到，近期工作站改动集中在点状勾画、轮廓保存、勾画面板/传播与AI面板拆分等方向。
- 只读检查代码可见病例列表里的 `已标注 N` 来自 `annotation_summary.completed_count`，它更接近病例级/模块级摘要，不等于每个序列、每帧、每个 ROI 的完成度。

#### 推测

- 这两天不应继续把主要精力放在科研指标、EDV/ESV/EF 或传播质量医学判断上。
- 更高价值的 QA 目标是确认医生实际会用的手工勾画链路是否稳定：能不能画、能不能保存、能不能显示、能不能取消/清空/撤销、刷新后是否保留、管理员是否能看到。
- `已标注1` 的显示问题可以继续记录，但应从“完成度不准”改成“管理员/列表摘要是否足以判断医生标注状态”的产品与数据可见性问题。
- 病例002的指标异常可保留为观察记录，但暂时不作为当前两天主线；等手工标注问题梳理完再回到 `传播与AI`。

#### 两天工作重心

- 第一天主线：只测手工勾画面板，不主动运行 AI 分割、传播补全、重算指标、清空全病例轮廓。
- 第一天重点：4CH 序列手工标注 1-2 帧的完整流程，包括选择 ROI、点状/闭合勾画、保存、刷新后加载、显示/隐藏标注、撤销/取消/清空当前帧。
- 第一天补充：SAX 只重点看脂肪 ROI 是否能手工勾画和保存；SAX 前 3 个 AI 可自动分割部位暂不作为医生手工标注主线。
- 第二天主线：管理员视角和医生标注结果可见性，检查管理员是否能看到不同医生/不同病例的标注状态、最近标注时间、标注来源和实际轮廓。
- 第二天补充：整理手工标注 bug 清单，再决定是否进入 `传播与AI` 的阶段性检查。

#### 暂停事项

- 暂不在正式病例上继续点击 `传播补全`。
- 暂不主动点击 `重算指标`，避免覆盖当前 observation。
- 暂不把负 SV/负 EF 作为唯一重点追查，先归档为“传播后 measurement 曾出现异常且后来变化，需要后续复核”。
- 暂不根据 `已标注1` 判断医生是否完成标注；必须打开具体序列和矩阵/轮廓确认。

#### QA 表格字段建议

- 病例编号 / 数据集
- 当前账号角色：医生或管理员
- 模块：4CH、SAX、LGE
- 序列与帧：slice、phase
- ROI/部位：例如 4CH 必标项、SAX 脂肪
- 操作：画、保存、刷新、显示/隐藏、撤销、取消、清空当前帧、切换病例
- 预期结果
- 实际结果
- 是否能保存
- 刷新后是否还在
- 管理员是否能看到
- 来源标记：手工、AI、传播、历史
- 是否存在“标了但取消不了”或“取消后仍显示”
- 截图编号
- 严重程度：阻塞、主要、轻微、待确认

#### 可向师兄同步的新口径

- 我接下来两天会先按你说的分阶段检查，第一阶段只聚焦手工标注和勾画面板。
- 我会优先测医生当前主要使用的 4CH 标注流程：画 1-2 帧、保存、刷新、显示/隐藏、撤销/取消/清空当前帧。
- SAX 这边我会只重点看脂肪 ROI 的手工标注链路；其他 AI 自动分割部位先不作为手工标注主线。
- `传播与AI` 和科研指标异常我会先记录、不继续扩散操作；等手工链路确认后再单独进入传播阶段检查。
- `已标注1` 我会按“列表摘要可能不足以反映医生实际标注状态”记录，后续用具体序列/帧/ROI 覆盖情况来核对。

## 2026-06-11

### ETA 勾画软件修改点评估

#### 已确认事实

- 师兄转发了 ETA 勾画软件的 4 个修改点：双线脂肪范围、冠脉走形擦除、自动识别心外膜界限并计算脂肪体积、放大后勾画范围不跟随。
- 本轮默认目标调整为“需求拆解 + QA 规格 + 汇报材料”，不是直接改生产系统。
- 已新增 `zian_workspace/eta_annotation_qa.md`，用于记录 ETA 修改点评估表、QA 场景、Bug 记录模板和给师兄的汇报草稿。
- 已新增 `zian_workspace/bug_assets/2026-06-11/` 作为本轮 ETA 相关截图/录屏证据目录。
- 已更新 `zian_workspace/questions_for_senior.md`，加入双线脂肪、冠脉排除、自动识别目标和缩放 bug 优先级等需要向师兄确认的问题。
- 只读检查 `apps/api/models.py`：当前 `FrameContour` 里有 `endo`、`epi`、`rv`、`fat` 等字段，但没有看到独立脂肪外层线字段。
- 只读检查 `apps/api/services/measurements.py`：当前心外膜脂肪体积来自 `fat` mask 的面积/体积累计，不是两条线之间自动相减。
- 只读检查工作站打包资源：当前 UI 有 `erase` / 橡皮擦相关痕迹，但是否能用于冠脉走形排除需要实际 UI 验证。

#### 推测

- 这 4 条意见中，放大后勾画范围不跟随最像可直接复现的前端坐标变换 bug，应优先录屏确认。
- 双线脂肪和冠脉排除不只是 UI 加按钮，可能需要新增 ROI 语义、保存结构和测量计算逻辑。
- 自动识别能力需要拆成 3 个问题分别判断：自动识别心外膜线、自动识别脂肪外层线、自动计算两线间体积。

#### 下一步

- 按 `eta_annotation_qa.md` 逐条收集证据。
- 优先复现 ETA-004 缩放坐标问题。
- 再验证 ETA-002 橡皮擦是否能真实保存并影响脂肪体积。
- 带着 `questions_for_senior.md` 中的医学/产品定义问题向师兄确认，不先自行定义脂肪内外层语义。

### ETA 录屏初步分析

#### 已确认事实

- 用户上传了两段 2026-06-11 的录屏到 `zian_workspace/bug_assets/2026-06-11/`。
- 两段视频均可被 OpenCV 打开，分辨率均为 1280x720，帧率均为 30fps，时长约 226-229 秒。
- 已抽取总览图、10 秒关键帧、局部时间段图和若干原始尺寸关键帧。
- 已新增 `zian_workspace/video_bug_analysis_2026-06-11.md` 记录视频分析结果。
- 视频中确认出现浏览器弹窗：“局部微调导致轮廓自交，请重画该局部轮廓。”
- 弹窗出现时，红色轮廓有明显自交/长线穿越；点击确定后，异常轮廓仍留在画面中。

#### 推测

- 该问题可能发生在局部微调、拖拽或控制点编辑过程中。
- 系统已有自交检测，但当前处理方式更偏提示用户重画，缺少自动撤销本次异常编辑或修复入口。
- 视频里还存在橙色脂肪 ROI 填充显示变化，但需要用户确认当时是否主动切换工具/隐藏/取消，才能判断是否为显示 bug。

## 2026-06-14 至 2026-06-15

### CMR/ETA 心外膜脂肪勾画专项：字段、计算与交付风险复盘

#### 已确认事实

- 本段记录整理于 2026-06-15；其中大部分代码实现、线上 UI 验证和热加载确认发生在 2026-06-14，2026-06-15 主要是补充复盘、梳理交付风险和准备与师兄沟通的问题。
- 本轮优化对象是真实 CMR 工作站：Flask 工作站页面加载 `apps/web/dist`，后端轮廓和测量由 `apps/api` 提供。
- 医生/师姐确认脂肪测量关注短轴 SAX 场景，脂肪内边界应是“心室外膜”，不是单纯沿用原系统里的 `epi=左室外膜`。
- 现有后端 `SeriesRole` 没有独立 `2CH` 或“二腔心短轴”角色，主要仍按 `cine_sax` / `cine_lax_4ch` / `lge_sax` / `lge_lax` / `unknown` 识别。
- 已新增并接入 `ventricular_epi`，作为脂肪专项的“心室外膜 / 完整心室外膜”轮廓字段。
- 当前脂肪体积计算逻辑已调整为：
  - 优先：`fat_outer - ventricular_epi - exclude`
  - 兼容：缺少 `ventricular_epi` 时退回 `fat_outer - epi - exclude`
  - 旧病例 fallback：缺少 `fat_outer` 或内边界时退回 `fat - exclude`
- `exclude` 当前表示 ROI 内部扣除区，会从当前脂肪原始 mask 内扣掉；如果 `exclude` 在脂肪区域外，不计入扣除体积。
- 后端保存前已增加闭合轮廓自交校验，覆盖 `fat`、`fat_outer`、`ventricular_epi`、`exclude` 等字段；自交时返回 400，避免异常轮廓写入数据库。
- 工作站 UI 已在当前本机/线上运行产物里显示 `心室外膜` 按钮；后续发现画完不保留，是因为前端还有一个硬编码轮廓 key 白名单漏了 `ventricular_epi`，已补入。
- 4CH 面板中脂肪专项按钮已从当前 dist 补丁里移除，避免干扰老师原来的 4CH 标注按钮。
- 后端 OpenAPI 检查中 `curl -s http://127.0.0.1:8010/openapi.json | grep -o ventricular_epi | wc -l` 返回过 `2`，说明当前 API 进程已加载 `ventricular_epi` schema。
- 已通过的验证包括：
  - `node --check apps/web/dist/assets/index-CVIBatchClear.pause-progress.js`
  - `PYTHONDONTWRITEBYTECODE=1 python3 -m py_compile ...`
  - `env MPLCONFIGDIR=/tmp/mpl PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -p 'test_function_fat_measurements.py' -v`
  - `env MPLCONFIGDIR=/tmp/mpl PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -p 'test_contour_save_guard.py' -v`
- 当前修改涉及后端 tracked 文件，例如 `apps/api/models.py`、`apps/api/services/measurements.py`、`apps/api/services/inference.py`、`apps/api/services/model_runner.py`、`apps/api/services/prompt_segmentation.py`、`apps/api/services/propagation_runner.py`、`apps/api/main.py` 和测试文件。
- 真实工作站 UI 的补丁位于 `apps/web/dist`，但该目录被 `.gitignore` 里的 `dist/` 忽略，正常 `git add/commit/push` 不会自动带上这些 dist 修改。
- `apps/api/services/dicom_indexer.py` 当前没有 Git 修改；编辑器里文件旁边的数字更像诊断/问题数量，不是 Git modified 状态。

#### 推测

- 当前几何公式本身符合“脂肪外层线 - 心室外膜线 - 排除区”的产品定义，但它计算的是两条线之间的全部 mask 区域，不会自动判断影像信号是否真的属于脂肪。
- 如果医生/师姐期待更接近 CVI42 的脂肪测量，后续可能需要在几何 mask 内增加脂肪信号阈值、灰度范围或半自动识别策略。
- 科研指标面板里“优先 ED”和“全序列”容易让肉眼验证产生误解：用户当前看的某一帧面积变化，不一定等于面板显示的 ED 或全序列体积变化。
- 当前 `exclude` 自交时，界面可能仍残留异常视觉轮廓；后端已阻止保存，但前端自动回滚体验仍需要单独修。
- 左右心室脂肪分割还没有完成。师姐提到的“清除一侧心室脂肪”更像 CVI42 的 side-specific fat 工作流，不是当前 `exclude` 的完整替代。
- 后续交付如果只提交后端代码，别人拉代码后可能没有 UI 变化；需要师兄确认前端源码在哪里、是否应恢复源码构建，或是否允许临时 force add dist。

### 四川省人民医院补充数据初检

#### 已确认事实

- 用户确认允许对 `/home/Larry/data/CMR_SCS_2/` 做数据接入初检。
- 目录内共有约 500 万个 DICOM 文件，未发现 CSV、Excel、JSON 等现成处理清单。
- 目录内有 4,865 个三级病例容器；其中 4,848 个可读 Study 容器、17 个无 DICOM 容器。
- 抽取的 DICOM 头显示 `PatientID` 与 `StudyInstanceUID` 对 4,848 个可读容器均完整且唯一；`AccessionNumber` 有 163 个空值和 841 条重复记录，不能作为全量唯一编号。
- 已在 `/home/Larry/data/CMR_SCS_2_workstation_intake/` 生成权限为 600 的私有导入索引，提供 `SCS2-0001` 至 `SCS2-4848` 的稳定检查顺序。
- 当前顺序临时按完整唯一的 DICOM `PatientID` 排序；`AccessionNumber` 仅保留为后续匹配字段。
- 现有导入器不适合直接导入 `CMR_SCS_2` 根目录，因为会递归扫描全部约 500 万 DICOM 并混合多个 Study。

#### 推测

- 工作站现有约 400 例很可能是筛选后的子集；这批数据规模更大，但重叠关系和序列完整性尚未验证。
- 在数据方确认 `PatientID` 的业务语义前，不应对外称它就是“登记号”。
- 新数据可作为原始来源使用，但还不能直接进行全量工作站导入；需先做每 Study 的 SAX/4CH/LGE 完整性与角色识别检查。

### 四川省人民医院 SCS 报告与 DICOM 对接

#### 已确认事实

- 检查了 `/home/Larry/data/` 附近的 `SCS_CMR_dcm_step6.xlsx`、`step7.xlsx`、`step8.xlsx`、`step8_cleaned_T.xlsx`。
- `step6` 有 860 条报告，属于后续 1,228 条报告集的子集；`step7`、`step8`、`step8_cleaned_T` 的检查号集合一致。
- `step8_cleaned_T` 是当前推荐报告对接来源：包含 1,228 条 `诊断结果`，649 条 `诊断意见`，以及处理后的 `Trans`、`Trans_3`、`Trans_4`。
- Excel 的 `检查号` 与 DICOM `PatientID` 精确匹配，不与 DICOM `AccessionNumber` 匹配。
- 已生成私有报告对接文件：1,174 条报告行精确挂到 1,173 个 Study；3,675 个 Study 当前没有报告；54 条报告行不能安全匹配。
- 已新增 `zian_workspace/bug_assets/SCS_CMR_2_报告与DICOM对接摘要.md`，记录报告字段、匹配规则、私有输出和后续流程。
- SCS 源目录仍是 raw DICOM；当前导入器会在导入单个 Study 时动态识别 SAX/4CH/LGE，但源数据没有持久化的角色分类清单。

#### 推测

- 1,173 个已链接报告 Study 是后续报告评分/原始报告展示的优先候选，但需先做序列完整性筛选。
- 54 条未匹配报告可能涉及缺失 DICOM、编号格式差异或身份归属冲突，应按昆医流程进行医院侧确认，不自动挂接。

#### 当前实现范围

- P0 防御：
  - 后端保存前拒绝自交闭合 polygon。
  - FastAPI 手工保存接口把自交错误转成 400。
- P1/P2 基础：
  - 新增 `fat_outer` 与 `ventricular_epi` 后端字段兼容。
  - 脂肪测量优先使用 `fat_outer - ventricular_epi - exclude`。
  - 输出脂肪净体积与排除区扣除体积。
  - SAX UI 暴露 `心室外膜`、`脂肪壁层`、`排除区` 等入口。
- 兼容：
  - 旧 `fat` ROI 仍可计算。
  - 缺少 `ventricular_epi` 的旧数据仍可退回 `epi`。
  - LGE 的 `exclude` 逻辑没有改成脂肪逻辑。

#### 尚未完成

- 真正的左/右心室脂肪分割：还没有 `lv_fat` / `rv_fat` 的保存结构、UI 工作流和科研指标。
- “清除一侧脂肪”工具：当前还不是 CVI42 那种一次去除单侧心室脂肪并保留左右侧体积的功能。
- 前端源码化：当前真实 UI 修改是 patch `apps/web/dist`，后续 rebuild 可能覆盖。
- 科研指标解释优化：面板还没有清楚显示 `未扣除总量 = 净脂肪体积 + 排除区扣除体积`，也没有显示扣除比例。
- 前端自交自动回滚：后端能拒绝保存，但前端在检测到自交后自动撤销异常轮廓的体验还需要继续修。
- 医学语义确认：是否需要在几何脂肪 mask 内加入信号阈值，仍需和师姐/师兄确认。

#### 如何验证当前功能

- 后端 schema：
  - `curl -s http://127.0.0.1:8010/openapi.json | grep -o ventricular_epi | wc -l`
  - 返回大于 0 表示 API 已认识新字段。
- UI：
  - 强刷工作站页面，确认 JS 参数为 `v=20260614-ventricular-epi-2` 或更新版本。
  - 打开 SAX/function，确认有 `心室外膜`、`脂肪壁层`、`排除区`。
  - 打开 4CH/function，确认脂肪专项按钮不再干扰原功能按钮。
- 计算：
  - 在同一帧只改变 `exclude`。
  - 观察 `心外膜脂肪体积`下降、`排除区扣除体积`上升。
  - 验证 `未扣除总量 = 心外膜脂肪体积 + 排除区扣除体积` 基本保持稳定。
- 异常：
  - 如果界面出现 `exclude 存在自交`，先撤销或重画该轮廓，再保存和重算，不把这次结果作为最终验证。

#### 和师兄吃饭时建议确认的问题

- 当前实习阶段的主要目标是什么：是修医生反馈、熟悉系统、做可交付小功能，还是为后续科研数据分析铺路？
- 真实前端源码在哪里？`apps/web/dist` 是不是只能临时 patch？正式交付应该改哪个源码目录、怎么 build、怎么部署？
- 当前线上/本地服务的热加载和部署方式是什么？改 FastAPI 后端、Flask 工作站、dist 前端分别需要怎样生效？
- `SeriesRole` 是否需要增加更细的序列类型，例如 2CH、3CH，还是短期继续用 `cine_sax`/`cine_lax_4ch`？
- 心外膜脂肪计算是否只要几何区域相减，还是需要在两条线之间再按影像信号阈值筛选脂肪？
- 师姐说的“一次清除单侧心室脂肪”在旧系统/CVI42 里具体怎么操作：是橡皮擦删除 mask、复制总脂肪后清掉一侧，还是保存左右侧独立 ROI？
- 左右心室脂肪结果应该怎么存：`lv_fat` / `rv_fat` 独立 ROI，还是总脂肪加清除记录？
- 科研指标最终需要哪些字段：净脂肪体积、未扣除总量、扣除体积、扣除比例、LV-side fat、RV-side fat、ED、all frames？
- 当前病例数据和标注数据的生产边界是什么？哪些测试病例可以安全写入，哪些病例不能动？
- 代码提交和上线流程是什么？需要谁 review？是否允许提交 dist？是否要写迁移脚本或版本号？

#### 希望从这段实习补齐的关键能力

- 读懂系统运行链路：前端页面、真实 dist、Flask 工作站、FastAPI API、数据库 payload、测量计算之间的关系。
- 把医生需求翻译成工程字段和计算公式：例如 `fat_outer`、`ventricular_epi`、`exclude`、`lv_fat`、`rv_fat`。
- 区分医学语义、产品语义和代码语义：不能把旧 `epi=左室外膜` 直接改成“心室外膜”而破坏左室功能计算。
- 小步验证能力：每次只改一个可验证点，配套单测、UI 操作和线上检查命令。
- 交付边界意识：知道哪些修改是 tracked code，哪些是 ignored dist，哪些需要 build，哪些不能直接上线。
- QA 复现能力：用截图、录屏、具体 slice/phase、错误提示和指标变化描述 bug。
- 和上游沟通能力：能清楚告诉师兄“已确认事实、推测、风险、下一步需要确认什么”。

#### 下一步建议

- 先不要继续扩大功能；优先把当前 `ventricular_epi + fat_outer + exclude` 链路在一个测试病例上稳定验证。
- 第二步补科研指标展示：显示未扣除总量、扣除体积、净体积、扣除比例，降低医生验证成本。
- 第三步和师兄确认前端源码/构建流程，解决 dist 修改无法正常 git 上传的问题。
- 第四步再设计左右心室脂肪分割和“清除一侧”工作流。


## 2026-06-25 17:02:02 +0800 DICOM匹配执行记录

- 任务：根据 `14-26.6.1分段CMR报告_合并脱敏.xlsx` 的登记号和检查时间，匹配完整昆医附二院 DICOM 库 `/home/Larry/data/CMR_ALL`。
- 已确认：Excel 3001 行；CMR_ALL 一级病例目录 2825 个。
- 匹配结果：FOUND_EXACT 2167 行；FOUND_ID_ONLY 280 行；AMBIGUOUS 159 行；MISSING 395 行。
- 前端状态：前端默认300例可见 230 行；需搜索或分页加载 2376 行；未匹配到DICOM 395 行。
- 输出文件：`zian_workspace/bug_assets/14-26.6.1分段CMR报告_合并脱敏_DICOM匹配标注.xlsx`；`zian_workspace/bug_assets/缺失DICOM病例清单.xlsx`。
- 未输出患者明细到对话。


## 2026-07-08 10:12:20 +0800 暑期正式实习目标校准

- 任务：根据用户与师兄沟通后的暑期安排，重写 `zian_workspace/AGENTS.md`，把旧的接手/迁移专项语境校准为暑期正式实习语境。
- 已确认：
  - 七月接下来约十五天，主要工作是维护 CMR/ETA 工作站、整理服务器数据、补齐未 link 到工作站的数据、继续维护医生端功能。
  - 主要沟通对象是医生，需要把医生反馈转译为可复现问题、需求优先级、UI 优化点和验收方式。
  - 会有几天线下去华西观摩 CVI 目标系统，重点学习真实使用体验、优化细节和方向。
  - 后续可能与组内另一位实习生对接 Agent 系统，将其缝合进当前工作站 UI。
  - 爱尔兰暑校期间按轻量远程维护处理，不默认安排高风险服务器操作或深度联调。
  - 八月可能进入论文制作，idea 和数据主要来自师兄/组里；师兄反馈目标可能是会议投稿，提到 CVPR，但需把“师兄反馈”和“已确认投稿计划”分开记录。
- 本次修改：
  - 更新新对话启动提示，把当前阶段目标改为“暑期正式维护和推进 CMR/ETA 工作站”。
  - 更新工作定位，明确工作站价值：医生平替工作站、科研人工标注数据积累、私有/公共数据整合壁垒。
  - 新增暑期阶段路线：七月数据与维护、华西 CVI 观摩、Agent 系统接入、爱尔兰轻量远程、八月论文准备。
  - 重写推荐接手路线，加入数据与工作站地图、医生使用流程、CVI 对标、Agent 对接问题清单、论文准备路线、师兄/导师问题清单。
- 未做：
  - 未修改项目根目录公共规则。
  - 未修改 `README_RESTART.md`。
  - 未执行 `git add`、`git commit`、服务重启、数据库写入或数据迁移。
- 下一步建议：
  - 先建立一份暑期任务看板或清单，按“数据 link / 医生反馈 / CVI 观摩 / Agent 对接 / 论文素材”五类管理。
  - 和另一位实习生对接 Agent 前，优先确认部署位置、调用协议、输入输出、异步任务、数据安全和 UI 缝合点。
  - 每次医生反馈都记录为“医生原话或现象 -> 复现步骤 -> 影响程度 -> 期望行为 -> 验收方式”。


## 2026-07-08 10:45:00 +0800 /home/Larry/data CMR数据资产整理启动

- 任务：对齐当前不再迁移到 r37 的新目标，开始整理 `/home/Larry/data` 下组内 CMR 数据资产，并生成 Markdown 信息汇总。
- 已确认：
  - 当前不再把数据迁移到 r37；HK 侧网络传输受限，后续跨服务器同步优先考虑电子科大服务器连接阿里云路线。
  - `/home/Larry/data` 总大小约 8.5T。
  - 顶层目录 30 个，顶层文件 65 个。
  - 主要工作站已配置数据根包括 `CMR_ALL`、`CMR_Chendu`、`CMR_SCS`、`CMR_YA/merged_files`、`CMR_RenJi` 的 HCM/MI 子目录。
  - 最大目录包括 `CMR_ALL_Image_new` 约 4.1T、`merlin_dataset` 约 3.0T、`CMR_ALL` 约 233G、`CMR_KM_Image` 约 216G、`CMR_RenJi` 约 197G、`CMR_KM_Image_seg` 约 154G。
- 本次修改：
  - 更新 `zian_workspace/AGENTS.md`，新增“当前近期任务对齐（2026-07-08）”。
  - 新增 `zian_workspace/cmr_data_inventory.md`，记录目录级数据资产、系统配置入口、初步分层、待确认问题和安全边界。
- 未做：
  - 未读取 DICOM 正文、患者字段、数据库正文、日志正文或密钥。
  - 未删除、移动、覆盖、下载、同步或 link 任何数据。
  - 未修改数据库、服务配置或公共 README。
- 下一步建议：
  - 和师兄确认各目录的来源、私有/公共属性、是否可重建、是否已 link 到 UI、哪些是论文/训练核心数据。
  - 建立顶层 CSV manifest，字段包括路径、大小、目录数、文件数、mtime 范围、分类、link 状态、待确认问题。
  - 根据 UI 页面逐个验证已配置医院数据是否可打开、可加载影像、可查看报告/结果、可进入标注或回看流程。


## 2026-07-10 15:23:48 +0800 工作站反馈闭环与方案校正

- 任务：修正 AI 将“实验结果页面风格不匹配”误判为“AI 专家侧栏问题”的反馈，并补齐方案在确认前可人工校正的工作流。
- 已确认：
  - 问题单现在可由反馈负责人修正标题、发生页面、摘要、实际/期望表现和验收标准。
  - 方案生成时可附加“修订说明”；提示词明确规定用户点名的页面/模块优先于截图中偶然出现的界面元素。
  - 草案方案可编辑处理步骤、风险控制、验证方法和 Codex 任务书；保存后才允许确认，确认后才可进入等待受控执行器的队列。
  - 修正已批准问题单会使方案回到草案，避免旧范围被继续执行。
  - 已通过后端语法检查、反馈 Agent 单测、前端生产构建与空 SQLite 建表验证；Flask 后端已重启，CVI API 健康检查正常。
- 未做：
  - 未读取或输出病例、数据库、日志、密钥或截图内容。
  - 尚未接入可直接改代码的 Codex 执行器；当前“进入执行队列”仍只记录批准状态，不会直接修改服务器。
- 下一步建议：
  - 在反馈看板将当前误判的问题单改为“实验结果页面”，填写修订说明后重新生成方案。
  - 之后为执行器补充路径白名单、测试门禁、差异审查、回滚和结果回写，才能实现网页内闭环执行。


## 2026-07-10 15:48:19 +0800 实验结果页面视觉修正

- 任务：将实验结果页面从装饰性分析页面调整为与 CMR 工作站一致的临床工作台风格。
- 已确认：
  - 页面改为深灰工作区、低圆角分区、细边界和黄绿色选中/状态色；移除了大标题卡片、背景渐变和装饰性光晕。
  - 顶部保留实验分析标题、评分病例库选择；统计表、实验图和评分区域使用一致的高对比表格/面板样式。
  - 移除了评分区内与临床操作无关的“结果解读”长段生成文字，保留原始评分、完成率、图表与统计指标。
  - 前端生产构建通过，`/experiment` 与 `/workstation` 本地页面均返回 HTTP 200；未重启后端或影像 API。
- 未做：
  - 未读取病例、数据库、日志、截图或密钥。
  - 未改变实验统计接口、评分计算或图像数据内容。


## 2026-07-12 12:41:21 +0800 CMR 工作站医生操作说明书交付

- 任务：依据当前最新版工作站，为医生编写从最小上手到进阶查阅的操作说明书，并同时提供可维护 Markdown 和医生阅读版 DOCX。
- 已确认：
  - 手册对应工作站版本 `v2026.07.10-1`，内容依据当前外层工作台、嵌入勾画页可见文案、AI 专家实现和更新日志整理。
  - Markdown 包含 3 分钟最小上手、病例导入/切换、SAX/4CH/LGE 勾画、ED/ES、AI 分割与传播、科研指标、AI 专家、各评估模块、常见问题、隐私和离开病例检查。
  - 医生版 DOCX 包含封面、快速目录、工作台布局图、六步流程图、提示/高风险信息框、统一表格、页眉和页码。
  - DOCX 已通过 OOXML 压缩包完整性检查；关键章节、16 个表格和 2 张示意图均已自动核对，示意图无明显文字裁切。
- 输出：
  - `zian_workspace/CMR工作站医生操作说明书.md`
  - `zian_workspace/CMR工作站医生操作说明书_医生版.docx`
  - `zian_workspace/tools/build_cmr_doctor_manual.py`
- 未做与限制：
  - 当前环境没有 Word、LibreOffice 或其他 DOCX 渲染器，因此未完成真实分页后的逐页视觉检查；首次在 Office/WPS 打开后仍需快速检查分页、字体替换和表格跨页表现。
  - 未读取或写入病例、DICOM、数据库、日志、截图、账号或密钥。


## 2026-07-12 15:50:51 +0800 后台病例分配与多中心数据浏览改造

- 任务：修正病例分配只支持昆医报告评分专项子集、分配方式隐式且危险的问题；对照数据盘点重做医院浏览的多中心接入口径。
- 已确认：
  - `CaseAssignment` 原模型已经按 `namespace + dataset + case_id + user` 隔离，不需要数据库迁移即可支持多数据中心。
  - 七个工作站已配置数据中心的原始候选合计约 9,638 个 Study/病例目录；四个旧 `new_CMR_*` output 一级目录合计 2,805 个，二者不是同一口径。
  - 旧医院树病种节点显示的是病种清单与 output 的匹配交集，同一病例可能跨病种重复，因此病种数不能相加作为医院唯一病例总数。
- 本次修改：
  - 病例分配自动列出工作站配置的多中心病例目录；昆医报告评分 100 例下沉为“专项子集”。
  - 支持按当前数据集库内序号、登记号/检查号、匿名病例号和病例 ID 解析；登记号对应多次检查时全部列入预览并提示。
  - 取消“未选人自动使用全部非管理员”；均分和共享改为显式选项；覆盖已有负责人和清空分配拆成独立操作。
  - 医院浏览新增多中心接入总览，拆分原始候选、工作站已登记、已导入 CVI、分类 output 目录；病种树拆分源清单数和当前可浏览数。
  - 管理员可访问医院浏览；`CMR_SCS_2` 和仁济 HCM/MI 在没有病种分类配置时显示“待分类”。
  - 更新 `cmr_data_inventory.md`，记录逐中心聚合数量、旧数字含义和昆医式分类所需条件。
- 验证：
  - Python 语法检查通过。
  - TypeScript/Vite 生产构建通过。
  - 病例范围、登记号、多次检查、目录层级和接入口径共 11 项相关单测通过。
  - `git diff --check` 通过；未执行任何真实病例分配、数据库写入、数据导入或目录变更。
- 部署状态：
  - 前端开发服务可热更新。
  - 2026-07-12 16:01（Asia/Shanghai）已按用户明确授权重启 Flask，当前后端进程正常运行。
  - `/api/hospital-browser/inventory` 与 `/api/admin/assignment-selection/resolve` 在无登录态下均返回 HTTP 401，确认新增受保护路由已加载；`/api/auth/me`、前端 `/admin/assignments` 与 CVI 健康检查均正常响应。
  - 尚未使用管理员登录态执行页面级真实分配或导入验收，未发生生产数据库写入。


## 2026-07-11 SCS_2 原始 Study 优先接入决策

- 师兄反馈：参考上海仁济医院病例库的接入方式，SCS_2 先导入工作站；后续由数据处理同学进行 SAX/4CH/LGE 等序列分类。
- 已确认：当前 SCS_2 是原始 DICOM 集合，已有 4,848 个可读 Study 的私有逐 Study 索引；不能把数据根目录作为一个 Study 导入。
- 已确认：工作站的病例库模式是先登记病例目录，医生在 UI 中对单例点击导入时才调用 CVI 导入接口；不需要先对全量数据分类。
- 已确认：SCS_2 的目录层级比现有仁济目录结构多一层，需先做最小目录适配，才能安全地按单个 Study 进行登记。
- 本次未做：未修改工作站配置或代码，未写数据库，未导入病例，未启动/重启服务，未修改 DICOM。
- 下一步：先完成只读的目录适配设计；师兄确认测试范围后，只用极小批量验证病例列表、单例导入、影像加载和报告链接边界，再讨论全量登记。


## 2026-07-11 SCS_2 病例库登记完成

- 已完成：新增 `CMR_SCS_2` 工作站病例库配置，并以私有导入索引将 4,848 个真实 Study 路径登记为匿名 `SCS2-0001` 至 `SCS2-4848`。
- 已完成：病例扫描器支持该数据源的三级目录结构；仅扫描私有索引中存在映射的 Study，不把中间目录或无 DICOM 容器登记为病例。
- 已完成：病例库刷新按数据集范围执行，避免刷新 SCS_2 时误扫描或删除其他医院的病例目录记录。
- 已验证：Python 语法检查通过；隔离临时目录测试通过；真实数据源扫描得到 4,848 个唯一匿名 Study；病例目录表登记后仍为 4,848 条，尚无预先导入 CVI 的 Study。
- 未做：未移动或修改 DICOM，未运行全量 SAX/4CH/LGE 分类，未批量导入 CVI 影像服务，未重启服务，未修改标注或报告内容。
- 已知验证缺口：当前 base Python 环境没有 `pytest`，因此新增测试文件未能通过 pytest runner 执行；已用等价的隔离 Python 断言完成扫描验证。
- 下一步：刷新工作站，确认“\u56db\u5ddd\u7701\u4eba\u6c11\u533b\u9662-\u8865\u5145\u6570\u636e”可见；再用 1-3 个测试病例进行 UI 单例导入和影像加载验证。


## 2026-07-12 16:25:34 +0800 工作站安全备份推送

- 任务：将当前工作站源码、前端和测试更新推送至 GitHub，同时排除病例及本机运行数据。
- 已确认：
  - 扩展 `.gitignore`，覆盖病例目录、临床导出、截图、标注、分割、mask、私有病例索引、DICOM/NIfTI 及常见医学数组格式；`zian_workspace`、`exports`、数据库、日志、密钥、模型权重和构建产物继续保持忽略。
  - 默认 CVI 样例目录改为 `CVI_DEFAULT_SAMPLE_PATH` 环境变量；健康接口不再回传本机目录；SCS2 数据根和私有匿名索引支持环境变量覆盖。
  - 提交快照中医学影像/数据库文件和密钥类文件计数均为 0，常见密钥特征扫描无命中，没有二进制或超过 1 MB 的新增文件。
  - Python 编译通过；40 项相关兼容测试通过；前端 TypeScript/Vite 生产构建通过，仅有既有的大 chunk 体积警告。
  - 提交 `bffad1c`（`feat: improve CMR workstation workflows`）已普通快进推送至 `TIaomiao/LabelSystemCd` 的 `main`；远端哈希已复核一致，本地工作区干净。
- 未做与边界：
  - 未读取、提交或修改病例、DICOM、生产数据库、日志、密钥和运行实例。
  - 未重写 Git 历史或强制推送；`.gitignore` 只能防止后续未跟踪文件进入 Git，不能清除历史提交中原本存在的源码示例或本机路径。
  - 当前会话没有 Notion 写入工具，本记录仅暂存于本地工作日志，且该目录不会推送。


## 2026-07-14 16:59:21 +0800 AI 专家模型与阅读体验增强

- 任务：提升工作站 AI 专家的模型能力、回复完整度、正文可读性和侧栏操作体验。
- 已确认：
  - “超高”档优先使用 `[j]gpt-5.6-sol` 并设置 high reasoning；“专家”档继续使用现有网关模型和 medium reasoning。
  - 超高模型不可用时会自动降级到现有网关模型，避免整次对话失败；可通过 `LABELSYSTEM_FEEDBACK_HIGH_MODEL` 覆盖高档模型标识。
  - 最低输出预算调整为专家 2400、超高 3200 tokens；提示词要求使用 Markdown 分段、编号步骤、结果检查和必要注意事项，不再强制压成一两句话。
  - AI 侧栏默认宽度为 540px，可在 420-760px 范围内拖动，支持方向键调宽、双击恢复默认并记忆浏览器设置；窄窗口按视口自动收缩。
  - 对话正文提高到 15px、行高 1.72，并补齐段落、标题、列表、引用、代码和表格的高对比样式。
- 验证：
  - 后端 Python 编译通过，反馈 Agent 6 项单测通过。
  - 前端 TypeScript/Vite 生产构建通过，静态行为契约与 `git diff --check` 通过；仅保留既有的大 chunk 体积警告。
  - 当前隔离会话没有浏览器/Playwright，也无法访问宿主运行服务，因此尚未完成登录态页面截图和真实 5.6 SOL 响应验证。
- 未做与边界：
  - 未读取网关密钥或敏感配置正文，未读取病例、数据库或日志。
  - 本轮尚未获得服务重启授权，源码修改尚未通过本轮重启加载；未执行 Git 提交或推送。
  - 当前会话没有 Notion 写入工具，本记录仅暂存于本地工作日志。


## 2026-07-14 21:46:00 +0800 昆医登记号 0000017513 SAX 缺失排查

- 任务：核对昆医登记号 `0000017513` 在 CMR 工作站中 4CH/LGE 可见但 SAX 不可见的原因。
- 已确认：
  - 该病例在 `CMR_ALL` 全部病例按 `case_id` 排序时位于第 1 位，因此“病例001”对应关系成立；但它不在当前“昆医附二院报告评分150例”配置清单中。
  - 当前已导入 CVI 的工作站记录使用不带 `DownLoadimg_` 前缀的 raw DICOM 目录，CVI study id 为 186。
  - 该目录下可识别并导入的 DICOM 仅来自 4CH 与 LGE 两个选定范围：4CH 25 张、LGE 7 张；CVI 内部只生成 `cine_lax_4ch` 和 `lge_sax` 两个 series。
  - raw 目录里存在 `SAX` 子目录，但该目录当前没有可识别 DICOM 文件；派生图像和 mask 候选目录也未发现可供当前 CVI 工作站导入的 SAX 原始序列。
- 推测：
  - SAX 不是被前端隐藏，而是当前 raw DICOM 接入目录中没有可用 SAX 文件，导入阶段自然无法生成 `cine_sax`。
  - 旧结果或旧页面中看到的“病例001”可能来自全部病例排序、旧派生目录或历史导入状态；需要回溯原始医院压缩包/历史备份才能判断 SAX 是源头缺失、清洗时漏拷，还是后续被清空。
- 未做与边界：
  - 未读取 DICOM 像素、日志正文、密钥或报告正文；未修改病例文件、数据库或服务。
  - 本记录只保存聚合状态和工程判断，不记录患者姓名或影像明细。


## 2026-07-14 21:47:03 +0800 昆医登记号 0000017513 数据库追溯补充

- 任务：在用户授权下进一步追溯 `0000017513` 的 CVI 后台数据库，核对“下午看到 SAX 传播标注但现在消失”的原因。
- 已确认：
  - 线上 CVI API 进程使用的是项目 `.cvi-workspace/cvi.db`，本次查询没有查错库。
  - `0000017513` 对应当前 CVI study 只登记了 2 个 series：4CH 与 LGE；没有 `cine_sax` series。
  - 该病例 raw 目录下的 `SAX` 子目录大小为 0，mtime 为 2025-03-17；没有今天被清空的文件系统迹象。
  - 当前 contour 表中，该病例只存在一条 4CH function 记录，更新时间为 2026-07-14T13:10:39Z；该记录仅保存相位/设置元数据，`frames` 为空，不是“大量传播轮廓”。
  - 今天下午/晚上确有多个 SAX series 被保存或传播过，其中包括较大的 SAX payload；但这些 series 属于其他 study，不属于 `0000017513`。
  - 早前讨论过的 `series 80/82` 属于另一个病例；其中 `series 82` 是 SAX 且 payload 很大，形态上更接近“很多传播标注”。
- 推测：
  - 用户下午看到的 SAX 传播标注很可能来自其他病例或旧筛选集合中的“病例001/相邻病例”，之后按登记号 `0000017513` 回查时打开的是当前 raw 目录缺 SAX 的病例。
  - 另一种可能是前端页面在快速切换病例/series 时保留了旧 canvas 或旧标注状态，但后端落库目标已经变成 `0000017513` 的 4CH series；需要 UI 复现才能确认。
- 未做与边界：
  - 本次没有修改数据库、病例文件或服务；没有恢复/删除任何标注。
  - 查询输出仅用于工程定位，工作日志不记录患者姓名、报告正文、DICOM 像素或敏感配置。


## 2026-07-16 22:20:00 +0800 最近几天工作日志覆盖情况与对话爬取索引

- 任务：检查 zian 的本地 worklog 是否记录了最近几天工作内容，并从已导出的本地对话/文档中整理可回看索引，方便后续进入 worklog 复盘。
- 已确认：
  - `zian_workspace/work_log.md` 已记录到 2026-07-14 晚上，覆盖了 AI 专家模型与阅读体验增强、昆医登记号 SAX 缺失排查、数据库追溯补充等工作。
  - 暂未在 worklog 中发现 2026-07-15 或 2026-07-16 的独立工作条目。
  - `zian_workspace/cmrworkstation_ai_expert_conversations_20260714/README.md` 记录了 AI 专家会话导出：共 4 个会话、52 条消息、26 条用户消息、17 个附件；数据库时间范围为 2026-07-10 至 2026-07-14 UTC。
  - `zian_workspace/cmrworkstation_ai_expert_conversations_20260714/session_4_ziantestpov.md` 中集中记录了一批工作站反馈/需求，不等同于已修复事项。
  - `zian_workspace/省医院CVI参观手册_2026-07-14.md` 已形成省医院/CVI 现场参观前的观察手册，包括系统介绍、观察顺序、参数清单、医生问题和汇报模板。
  - `zian_workspace/cmr_data_inventory.md` 已记录 CMR 数据资产、已配置工作站的数据入口、SCS_2 登记状态、多中心数据浏览口径和待确认问题。
- 从 AI 专家对话中可回看的待办/问题索引：
  - LGE 病灶识别及测量板块需求：需区分“现有 LGE 分析汇总展示”与“新增自动病灶识别”。
  - AI 反馈助手体验：需要 Markdown/换行渲染、重点文本样式、论文文件上传与工作站能力比对。
  - 影像质量、功能评估、结构评估、LGE 分析、其他发现等页面出现“无数据/暂无图像/无可用矩阵”类显示异常。
  - 报告评分阶段希望加入不同颜色的关键词标记，需进一步定义颜色类别与验收标准。
  - 工作站影像页右侧科研指标面板出现 EDV、ESV、SV、EF、CO、CI、心肌质量等结果为空或横线的问题。
  - 顶部“当前病例-上一位/下一位”切换条可能与左侧病例入口重复，占用影像工作区。
  - 目录浏览入口对医生无明确价值，且存在服务器/本地路径信息泄露和误操作风险，需要高优先级复核。
  - 当前重新打开同一病例/同一文件不方便，可能需要“重新打开/重新加载当前病例”按钮。
  - Slice / Phase Matrix 中医生手动 ED/ES 或“手”标签显示不稳定，刷新后有时消失。
  - 影像页临时画线工具需求：可临时画线、随时清除，并且不写入或干扰分割 label/ROI。
  - UKB 字段库连接失败仍缺触发入口信息，需要补充页面/按钮路径。
  - 框选+SAM、涂色+SAM 与擦除类工具的鼠标提示相似，可能导致医生误操作。
- 推测：
  - 7 月 14 日下午的 AI 专家反馈对话里积累了大量“医生/用户视角问题”，但 worklog 之前只记录了模型与阅读体验增强，没有把全部反馈条目整理成索引。
  - 7 月 15-16 日如果有真实工作内容，可能还在其他未导出的对话、临时笔记或记忆中；当前本地 worklog 尚不能证明这些日期已经完整记录。
- 未做与边界：
  - 本次只读取 `zian_workspace` 下的 Markdown/文件列表和已导出的对话 Markdown，没有读取附件图片实体、数据库正文、日志正文、DICOM、患者数据、密钥或 token。
  - 本索引只用于复盘和后续排查排队，不代表这些问题已经复现、定责、修复或上线。


## 2026-07-16 15:34:45 +0800 AGENTS 本地工作日志规则调整

- 任务：按用户要求删除 `zian_workspace/AGENTS.md` 中的 Notion 写入规则。
- 已确认：
  - 工作日志规则已改为默认只维护本地 `zian_workspace/work_log.md`。
  - 保留了按时间顺序追加、轻量记录、避免敏感内容和只记录阶段性成果的要求。
- 未做与边界：
  - 未修改项目根目录 `AGENTS.md`、公共 README 或上游协作规则。


## 2026-07-16 16:52:24 +0800 省医院室间隔曲率手工 V1 上线

- 任务：依据省医院补充资料，从 0 到 1 实现并上线 SAX cine 室间隔/左室游离壁四点曲率测量。
- 已确认：V1 由医生手工选择基底段中间层和收缩末期帧，依次放置 J1、J2、M1、M2；输出 R IVS、R FW、带符号 C IVS、C FW 和 RC。M1/M2 位于参考弦同侧时按反弓记负，不含 SBP、RVSP 和 0.67 阈值判读。
- 已确认：原内层 Viewer 的 React 源码无法从本仓库及已知备份恢复，因此采用“冻结现有 Viewer 运行时 + 独立可维护曲率扩展”的兼容实现，保留现有勾画、传播、测量和报告功能。
- 已确认：后端提供曲率预览和专用四点保存接口，优先读取所选帧原始 DICOM PixelSpacing；旧轮廓保存和模型任务不能覆盖已保存四点，并加入并发写入保护。
- 已验证：曲率后端专项测试 23/23、完整后端测试 91/91、曲率前端测试 9/9、TypeScript/Vite 构建、外层主站构建及 `git diff --check` 均通过。
- 已上线：曲率 JS/CSS 已安装到工作站，CVI API 已精确重启且健康检查正常；实际运行接口清单包含曲率预览和保存路由，公网 `/workstation` 入口可访问。部署时发现同期 LGE Scar 显示开关更新，已完整保留并同步，未回退旧 Viewer 功能。
- 交付文档：`docs/curvature_manual_v1_user_guide.md`；包含选层、选帧、四点放置、结果解释、常见提示、修改/清除和 V1 验收流程。
- 未完成与边界：尚未在登录态浏览器内用去标识化的正常、变平、反弓病例各跑一例端到端验收；当前结果仍标为研发草稿，不含自动选层/选相/自动放点、正式确认历史或电子签名。
- 推测：暂无。


## 2026-08-13 11:56:03 +0800 Tissue LGE 验收修复上线与版本控制审计

- 任务：修复 Tissue LGE 按钮范围、主序列勾选、Series Overview 的 Matrix 异常和“重新打开”误触发强制重导入，并为真正重导入补人工状态保留测试；同时审计 Git/GitHub 备份与回滚能力。
- 已实现：非 `lge_sax` 卡片彻底隐藏 Tissue 按钮；当前主序列显示 `Tissue LGE ✓`，切换时两张 LGE 卡片原地更新，不再整页刷新；Overview 可见时稳定隐藏 Navigator 和 Slice / Phase Matrix。
- 已实现：病例列表中的普通“重新打开”固定使用 `force=false`，只复用现有 Study 并刷新 iframe；浏览器验收确认操作前后 Study ID 均为 719，没有重新建索引。
- 已实现：真正重导入时按完全一致的 Series UID 保留人工 role 和唯一 Tissue LGE 主标记；不做模糊匹配，避免标记迁移到错误 LGE。轮廓、测量和报告的原有迁移逻辑保持不变。
- 已验证：15 项后端隔离导入/重导入测试、12 项工作站前端测试、主站 TypeScript production build、工作站 staging build/校验均通过；测试只使用临时 SQLite 与合成 DICOM。
- 线上验收：版本升级为 `v2026.08.13-1`；浏览器中 3 张 Overview 卡片只显示 1 个可见 Tissue 按钮、Matrix/Navigator 可见数为 0。当前验收病例后端没有主标记，因此活动勾选数为 0，符合接口状态。
- 已部署：源码和 frozen runtime 同步至 `apps/web/dist`，仅重载 CVI API 8010，新 PID 为 638547；Flask 5000 与 Vite 5173 未重启。三个服务健康请求均返回 200。
- 回滚：上线前文件备份位于 `/home/Larry/backups/LabelSystem/tissue_lge_acceptance_fix_20260813T112953/`，可按文件恢复本轮源码和 frozen runtime；未改生产数据库或病例内容。
- 已确认版本控制现状：服务器仓库 `main` 为 `c46a315`，GitHub `origin/main` 为 `bffad1c`，服务器领先 7 个提交；GitHub 最后可确认的 main 更新为 2026-07-12。当前 Tissue LGE 及其他大量改动仍在未提交工作树中，既没有 Git commit，也没有推送到 GitHub。
- 已确认回滚能力：仓库已有 Git 历史、发布检查清单和文件级上线备份，但当前没有“任意线上版本一键回滚”的可靠能力；脏工作树和 frozen runtime 热补丁使单纯切 Git commit 不能完整还原现网。当前最可靠的是按发布备份恢复指定文件。
- 下一步建议：整理本轮及现存脏改动的归属，建立独立 release commit/tag、发布清单与可执行 rollback 脚本，再经用户明确授权推送 GitHub；不要在未拆分师兄与用户改动前直接整体 commit/push。
- 推测：暂无。


## 2026-07-16 16:55:39 +0800 左房功能阶段状态与验收准备

- 医生需求：在长轴工作站补充左房功能测定，覆盖容积、排空功能和应变，并在左室功能基础上形成可集中查看的左房板块。
- 已确认：源码已实现 4CH 单平面 LAVmax/LAVpre-A/LAVmin、总/被动/主动排空分数、左房容积曲线和基于轮廓周长变化的应变 proxy；已加入 2CH/3CH 序列角色、2CH+4CH 双平面左房容积、手工 BSA 和 LAVi，以及左房相邻人工相位的光流追踪验证。
- 已验证：左房专项测试 11/11、完整后端测试 91/91、工作站前端测试 9/9、TypeScript typecheck、运行补丁 JavaScript 语法检查和 `git diff --check` 均通过；当前 FastAPI `/health` 正常。
- 待验收：尚未由 zian 在登录态测试病例中完成保存、刷新、重算、关键相位、曲线、BSA/LAVi 和回归流程验收；真实 2CH 数值仍待数据到位后验证，左房勾画边界和关键相位口径仍需医生确认。
- 运行状态：磁盘前端版本为 `v2026.07.16-4`；宿主侧复核确认 8010 API 于 16:49 从当前仓库启动，OpenAPI 已包含 2CH/3CH、左房关键相位和曲率接口。此前隔离命令得到的 2CH/3CH 计数为 0 属于沙箱网络视图误判，不代表宿主服务版本。相关修改仍位于脏工作区，尚未形成可复现版本提交。
- 推测：暂无。


## 2026-07-16 17:32:26 +0800 左房功能演示排查与讲稿

- 任务：排查左房面板仅显示 LAVmax/LAVmin、排空分数和应变分量为空的问题，并准备医生操作演示稿。
- 已确认：测试截图中 `LAVmax=23.34 mL`、`LAVmin=47.73 mL`，实际为 max 小于 min；系统按设计判定关键相位/轮廓顺序异常并抑制负排空分数。`LA pre-A` 未设置，因此被动、主动排空分数也缺少输入。
- 已确认：LV ED/ES 与 LA max/min 是两套标签；LV ED 通常近似对应 LA min，LV ES 通常近似对应 LA max。要测试左房错误保护，应反设 LA 标签，而不是改 LV ED/ES。
- 输出：新增 `zian_workspace/左房功能操作演示稿_2026-07-16.md`，包含输入条件、相位解释、修正步骤、计算公式、两分钟逐句讲稿、错误保护演示和录制前检查表。
- 待优化：相位顺序错误提示不够醒目；修改后指标自动刷新不稳定；缺少 2CH 时 BSA 保存成功文案不应声称 LAVi 已重算。
- 未做与边界：未修改线上代码、服务、病例轮廓、数据库或生产数据。
- 推测：暂无。


## 2026-07-17 19:50:33 +0800 脂肪灰度筛选与 LGE 演示成片

- 任务：审核两段工作站操作录屏，裁掉脂肪视频结尾卡顿，校正术语并生成可发医生群的中文字幕 MP4。
- 已完成：脂肪视频裁至 154.05 秒，保留 4CH 二维预览说明，删除后续长停顿和 `sorry`；LGE 视频完整保留。
- 已完成：人工校正 SAX、4CH、epi、心室外膜、脂肪壁层、Scar、Grey Zone、n-SD 和 FWHM 等字幕，并补充排除区即时生效、Scar 显示开关不改变计算等说明。
- 隐私处理：医生群版对左侧病例列表做重度模糊和深色覆盖，未覆盖原始 MKV。
- 输出：`zian_workspace/New function/操作演示/成片/脂肪灰度筛选_医生群版.mp4`、`lge阈值_医生群版.mp4`、两份 SRT 及 `医生群发送文字补充.md`。
- 已验证：两段 MP4 均为 1280x720、30fps、H.264/AAC；全片解码检查无错误，抽查首尾和关键字幕正常。
- 工具：已将校验通过的用户级 `ffmpeg` / `ffprobe` 安装到 `~/.npm-global/bin`；未使用 sudo 密码或修改系统包。
- 推测：暂无。


## 2026-07-17 23:01:58 +0800 省医院 PAH 曲率验收病例库上线

- 任务：将省医院提供的 PAH-TEST 曲率验收数据接入 CMR 工作站，并只授权指定医生账号验收。
- 已确认：数据源包含 16 个顶层病例、3932 个 DICOM 对象；其中 3925 个为可显示 MR 图像，7 个为 Raw Data Storage。导入器已改为同时校验 Image Storage SOP Class 和正数 Rows/Columns，Raw Data 即使带矩阵尺寸也不会作为图像帧导入。
- 已确认：16 例均已建立独立生产 Study 并写回病例目录；总计 16 个 `cine_sax` series、3925 帧、每例 25 相、7 至 13 层，帧数均等于层数乘相数。重复 StudyInstanceUID 的不同目录和缺失 StudyInstanceUID 的病例均保持独立且可重复导入。
- 已确认：15 例 PixelSpacing 为 1.25×1.25 mm；PAH-222-2 为 1.198762×1.198762 mm，已逐帧核对为原始 DICOM 自带值，不是导入误差。
- 权限：新病例库 `CMR_SCS_PAH_TEST` 按分配默认拒绝。16 例均精确分配给“彭盛坤-四川省人民医院”（user_id 37），其他普通账号有效分配为 0。Study、Series、图像、轮廓、测量、报告、导出、模型任务和 job 查询/暂停均复用对象级门禁；普通账号不能直接浏览服务器目录或绕过病例库导入。
- 生产验收：指定医生可见 16/16、全部显示已导入，病例页、工作站页和曲率预览均返回成功；合成四点预览几何状态为 valid、QC 为 pass、RC 为有限数值，并明确 `persisted=false`。保存路由用空请求验证已到达上游参数校验，没有写入假四点。未分配普通账号可见 0 例，病例导入、Study、曲率预览和保存均返回 403。
- 数据完整性：验收库当前曲率轮廓记录和测量记录均为 0；技术冒烟没有留下临床标注。原始 DICOM 未修改、未复制，工作站只保存索引和医生后续产生的测量结果。
- 验证：完整后端测试 123/123、曲率前端测试 9/9、曲率扩展构建、主前端生产构建、`py_compile` 和 `git diff --check` 均通过；CVI API、LabelSystem 后端和既有前端健康，未重启主前端、Showcase 或 FRP。
- 交付：新增 `zian_workspace/省医院参观/曲率功能医生验收资料/PAH-TEST_曲率验收入口与流程_2026-07-17.md`；原验收 PDF 继续使用 `省医院_室间隔曲率手工测量V1_验收说明_2026-07-16.pdf`。
- 当前边界：尚未由临床医生在 16 例上完成真实 J1、J2、M1、M2 放点及数值口径确认；系统未代替医生写入任何真实曲率结果。当前版本仍不计算 SBP、RVSP，也不按 0.67 阈值自动判阳性。
- 推测：暂无。


## 2026-07-22 14:34:23 +0800 省医院 SCS_PAH-1 曲率验收数据接入

- 任务：将 `/home/Larry/data/SCS_PAH-1/第一大类SAX` 接入工作站，开放给“彭盛坤-四川省人民医院”和系统管理员，并完成导入、权限及在线图像验证。
- 已确认：新增私有病例库 `CMR_SCS_PAH_1`，目录扫描得到 001—041 共 41 例、10077 个 DICOM 对象；014 的 12 个子序列目录保持为一个病例，没有被误拆。
- 已确认：10050 个 MR Image Storage 对象进入工作站，27 个 Raw Data Storage 对象被正确排除；41 例均只有一个 `cine_sax` series、均为 25 相，层数范围为 4—13。重点病例 006 为 4×25，014 为 12×25。
- 去重处理：9 例与旧 PAH-TEST 影像按 Study、Series、SOP UID 及逐文件 SHA-256 全部相同，复用旧 Study；005 虽与旧病例共享 Study UID，但 Series/SOP 零重合，已按独立 Study 导入。最终为 9 例复用、32 例新导入、41/41 linked。
- 权限：新库启用按分配默认拒绝；建立 41 条精确有效分配，唯一普通账号为彭盛坤。管理员使用既有角色旁路，不建立逐例分配；未分配普通账号在病例库中看不到新库，并且 Study、图像、导入和旧详情接口均返回 403。
- 线上验收：彭医生和管理员均可见 41 例，样例 007 的 Study、单一 SAX series 和真实 PNG 图像均返回 200；未分配普通账号对应请求均为 403。41 例合计 41 个 Study、41 个 SAX series、10050 帧，旧 PAH-TEST 16 例保持完整。
- 数据安全：接入前已分别备份 LabelSystem 与 CVI SQLite 数据库并通过 `integrity_check`；原始 DICOM 未修改。技术验证未写入任何 contour、四点或 measurement，新库当前相关记录均为 0。
- 验证：权限专项、目录扫描和导入专项均通过；使用正确 `PYTHONPATH` 的完整后端测试 131/131 通过，`py_compile` 与 `git diff --check` 通过。CVI API 与 Flask 已精确重启并健康，前端、Showcase 和 FRP 未重启。
- 交付：新增 `zian_workspace/省医院参观/曲率功能医生验收资料/SCS_PAH-1_曲率验收入口与流程_2026-07-22.md`。
- 当前边界：临床医生尚未在这些病例上完成真实选层、选帧和 J1/J2/M1/M2 放点，系统不代替医生产出临床曲率结果；仍不计算 SBP、RVSP，也不按 0.67 阈值自动判阳性。
- 推测：暂无。


## 2026-07-23 11:44:26 +0800 杨楠省医院主库权限开通

- 任务：按用户选择的范围 1，为账号“杨楠”开放四川省人民医院常规主库，排除 PAH 验收数据。
- 已完成：为 `CMR_SCS` 当前 482 个目录病例建立 482 条精确有效分配；线上病例库验证可见 482/482，已导入样例的 Study 和导入接口均返回 200。
- 已排除：`CMR_SCS_2`、`CMR_SCS_PAH_TEST`、`CMR_SCS_PAH_1` 对杨楠的有效分配均为 0；两个 PAH 库的已导入 Study 线上验证均返回 403。
- 权限边界：`CMR_SCS` 沿用现有常规共享库规则，分配控制病例库入口和导入，不把整个主库改成按分配私有，避免未经授权撤销其他医生既有共享查看能力。
- 数据安全：变更前已备份 LabelSystem SQLite 数据库并通过 `integrity_check`；未修改原始 DICOM、标注或测量，不需要重启服务。
- 推测：暂无。


## 2026-07-23 14:30:12 +0800 数据集权限管理面板与权限语义修正

- 任务：在管理员后台增加真实的数据集权限分配面板，并修复“某某专属”看起来像新建数据集、实际只是病例列表显示名的误导。
- 已实现：新增独立 `DatasetAccessGrant` 整库权限关系，与 `CaseAssignment` 病例级任务严格分开；管理员自动继承全部数据集，整库权限自动包含未来新增病例，撤销整库权限不会删除既有病例任务。
- 已实现：后台“数据权限”入口展示真实数据集名称/标识、目录病例、含 DICOM、已导入和整库授权账号数；明确标出严格私有库、常规共享库、系统共享库，以及“当前全覆盖但非整库权限”的账号。
- 已修正：工作站病例库不再生成“用户名专属”名称，改为“数据集 · 整库权限”或“数据集 · 已分配病例（N例）”；杨楠当前 482/482 仍属于病例级全覆盖，不会被伪装成新数据集，也不会自动继承未来病例。
- 权限边界：严格私有库的病例列表、导入、Study、Series、图像、轮廓、测量、报告和导出均接受整库权限并继续默认拒绝未授权账号；常规共享库保留已有的已知 Study 共享规则；系统共享库不允许重复授权。
- 风险修正：拒绝未知数据集、未审核用户、管理员重复授权、布尔或小数用户 ID；非管理员不能读取或修改面板；保存后立即清除监控缓存，避免显示旧权限数。
- 验证：数据权限专项测试 11/11、完整后端测试 142/142、后端 `py_compile`、前端 TypeScript/Vite 生产构建和 `git diff --check` 均通过。
- 当前边界：本轮只完成源码和测试，未部署、未重启服务、未改线上权限数据；上线后需由管理员在面板明确勾选，才会把病例级全覆盖升级为真正整库权限。
- 推测：暂无。


## 2026-07-23 15:42:00 +0800 数据集权限模块 v2026.07.23-1 正式上线

- 任务：完成数据集权限后台的正式上线、日志与版本留痕，并把杨楠从 `CMR_SCS` 当前病例任务全覆盖升级为真正整库权限。
- 上线：LabelSystem Flask 与 Vite 已切换到 `v2026.07.23-1`；新增授权表、追加式审计表和拒绝审计更新/删除的 SQLite 触发器均已生效，前后端本地及公网入口正常。
- 权限：杨楠原有 482 条 `CMR_SCS` 病例任务保持不变，新增且仅新增 1 条 `CMR_SCS` 整库有效授权，未来新增病例自动包含；`CMR_SCS_2` 和两个 PAH 库均无整库授权。
- 审计：正式接口产生且仅产生一条 `grant` 审计，管理员、目标账号、真实数据集、版本和来源地址均正确；重复保存相同名单没有新增审计，生产审计 UPDATE/DELETE 均被数据库触发器拒绝。
- 隔离验证：杨楠读取主库为 200、管理接口为 403，补充库和两个 PAH 库导入均为 403，两个 PAH Study 直链均为 403；数据库上线后完整性为 `ok`。
- 备份：上线前数据库与源码包均保存在仓库外的受控目录，权限为 `600`，数据库完整性与源码包压缩完整性均通过；校验和记录于 `docs/releases/v2026.07.23-1_deployment_record.md`。
- 运行观察：第一次按计划只切换 LabelSystem 前后端；随后发现另一个 Larry 远程会话在 15:39:52 重新拉起了整套既有服务，造成 CVI API、Showcase 和 FRP PID 同步变化。已针对最终进程重新完成健康、权限和隔离验证，结果正常。
- 版本口径：后台权限模块版本为 `v2026.07.23-1`；工作站顶部仍显示临床功能版本 `v2026.07.16-4`，本次没有修改曲率或其他临床测量算法。
- 交付：`docs/dataset_access_admin_guide.md`、`docs/releases/v2026.07.23-1.md`、`docs/releases/v2026.07.23-1_source_manifest.md`、`docs/releases/v2026.07.23-1_deploy_rollback_checklist.md`、`docs/releases/v2026.07.23-1_deployment_record.md`。
- 当前边界：尚未创建 Git release commit/tag，实际发布源码以基础 commit、规范化快照 SHA-256 和受控源码包 SHA-256 共同标识；独立人工复核与业务验收签字待后续补录。
- 推测：暂无。


## 2026-07-29 20:08:16 +0800 多账号整库权限补齐

- 任务：按明确授权，为一批医生账号开放工作站当前全部数据集权限。
- 已完成：8 个申请账号中，5 个可按输入的精确用户名唯一匹配且为已审核普通账号；已为这 5 个账号补齐 8 个可授权数据集，共新增 40 条有效整库授权。系统共享库继续由已审核账号自动继承。
- 未完成：其余 3 个输入名称在系统中没有同名账号，未猜测映射、未创建账号、未写入权限，待确认真实登录用户名或完成注册后再处理。
- 安全核验：通过正式管理员接口在现有授权名单上取并集，没有撤销其他账号权限；新增 40 条追加式授权审计；目标账号原有病例任务数量保持不变，数据库完整性检查为 `ok`。
- 验证：数据集权限专项测试 18/18 通过；最终权限总览确认 5 个账号均覆盖 8 个可授权数据集，已有整库授权全部保留。
- 当前边界：本次没有改代码、重启服务、修改病例、标注或测量；全新增加的数据集仍需管理员另行授权，不会被已有整库授权自动继承。
- 推测：暂无。


## 2026-08-04 09:47:54 +0800 省医院 EHR 样例与工作站检索只读盘点

- 任务：只读统计省医院病历导出规模，审计 10 例治理样例的结构、脱敏边界与可提取字段，并核对 CMR Workstation 的省医院检索和 EHR/影像 link 现状。
- 已确认：原始病历导出根下有 39,560 个一级病例目录，全部采用同一 `ZY` 加 14 位数字的目录键格式；本轮未进入这些目录、未读取病例或影像正文。
- 已确认：治理目录包含 10 个病例，但不是 10 个文件；它同时保存 EHR 结构化层、128 份发布 PDF 和 OCR/PII 检测/盖章移除/烧录脱敏/复核等中间产物，共覆盖 128 份文档、752 页。
- 脱敏边界：自动泄漏扫描对 10 例全部通过且记录 0 个残余命中，但人工 QA 明确说明病例号、住院号和床号仍保留；病例目录名也保留院内业务键。机构名称不属于当前 PII 检测类别，整个治理根不能当作可直接对外发布的完全脱敏数据集。
- 可提取数据：当前结构已经覆盖时间线事件、诊断、检验值/单位/参考范围/异常标志、医嘱及剂量途径、影像 modality/部位/检查号/报告时间/所见/结论，以及手术、病理、麻醉、输血和页码/bbox 溯源；10 例中共有 124 条结构化影像报告记录。
- Link 现状：124 条影像记录的本地 DICOM 候选数均为 0，状态均为无本地索引匹配；仓库源码也未接入该 EHR 治理目录。因此 CT 等报告目前只是完整住院 EHR 上下文，不代表已经进入或成功关联 CMR 影像库。
- 检索现状：主工作站病例库默认只检索病例 ID、数据集和完整目录 ID；普通省医院主库没有 ID manifest。补充库已支持通过 manifest 检索 PatientID、AccessionNumber、Study ID 和 StudyInstanceUID；旧医院浏览器虽配置部分医院字段，但只筛选已匹配到 MRIAgent output 的病例。
- 风险与下一步：当前导入器没有按 DICOM Modality 强制限定 MR，且同一目录混入多个 Study 或 CT/MR 时存在合并到一个内部 Study 的风险。与师兄应优先确认 `ZY` 字段定义并索取一条脱敏 crosswalk，覆盖匿名病例 ID、源相对目录、PatientID、住院/就诊号、AccessionNumber、StudyInstanceUID、日期、Modality 和报告 ID，再决定批量提取与 link 规则。
- 未做：未读取原始病例目录内容、数据库、PDF/影像正文或 DICOM 标签，未修改应用代码、服务、生产数据或权限，未执行部署和测试。
- 推测：`ZY` 在本批数据中已确认是 EHR 导出病例目录键，但其医院业务系统中的正式字段名仍未确认；最终发布 PDF 中是否仍可见医院抬头需要在授权的隔离环境做定向视觉复核。


## 2026-08-05 16:21:11 +0800 肺癌 EHR OCR 原型边界纠正

- 任务：纠正初学者演示把单份 PDF OCR 误解为完整病例治理、把基础规则扫描误称为脱敏门禁通过的问题。
- 已确认：原演示暗中选择了一份单页文档；内容实际为 ECG，而不是病例级入院/出院诊断，无法覆盖同病例的病历、检验、医嘱、手术和病理信息。
- 已修改：输出正文改名为 `review_text_NOT_SAFE.md`；固定标记脱敏未完成、必须人工隐私复核、禁止外部 API；旧 `safe_text.md` 派生输出已由重新运行清理。
- 已修改：增加报告签名者基础替换规则和 ECG 内容优先分类；真实单页回归仅核对安全统计，已分类为 ECG，并命中报告签名替换规则，未输出正文到对话。
- 已修改：演示入口支持 `--list`、`--case N` 和 `--random`，只显示 `CASE-01` 等匿名别名；选择一例后处理该病例目录下全部 PDF，并以 `DOC-XXX` 保存逐文档结果。
- 已确认：10 例样例每例包含 8–18 份 PDF；匿名列表功能不读取 PDF 正文。
- 当前边界：病例级时间线、诊断角色、检验、医嘱、影像、病理/手术/麻醉、bbox 溯源和质量问题仍明确标记为未实现，不得把已有治理数据库冒充新脚本输出。
- 验证：9 个合成单元测试通过，脚本语法检查通过；未擅自运行任意完整病例 OCR，等待用户自行选择病例。
- 推测：暂无。
## 2026-08-08 12:47:16 +0800 昆医勾画任务病例影像打开问题定位与修复

- 任务：按《勾画任务分配.xlsx》中的实习生登记号，定位 CMR Workstation 中“服务器有图像但系统打开无图/未 link 好”的原因，优先处理昆医 CMR_ALL 已安排任务病例。
- 已确认：Excel `Sheet1` 共 1098 个登记号，分配给 5 位实习生；本地 `/home/Larry/data/CMR_ALL` 可按登记号匹配到 1013 个唯一登记号、1124 个病例目录，85 个登记号当前未在该根目录下匹配到病例目录；36 个已匹配登记号存在非 UTF-8 乱码路径。
- 已确认：主要工程根因是旧昆医目录结构常为 `病例目录/SAX/具体序列目录/*.dcm` 和 `病例目录/4CH/具体序列目录/*.dcm`，CVI 导入器此前只用“具体序列目录名”识别角色，未纳入父目录 `SAX` / `4CH`，导致导入后 SAX/4CH 多为 `unknown`，前端 Function SAX 无法选中可显示序列。
- 已确认：`0000664462` 的 UTF-8 报错来自病例内少数乱码目录名；这些路径可被 Linux 文件系统打开，但不能作为合法 UTF-8 写入 SQLite/JSON，导致整例导入失败。
- 已修改：CVI 导入器角色识别纳入直接序列父目录；导入时跳过 surrogateescape 乱码路径，避免少数坏目录中断整例；后端病例库 DICOM 计数同步排除这类不可入库路径；统一工作站“重新打开”入口现在会强制重导入已有 study。
- 验证：用临时 CVI 数据库导入 `0000130509`、`0000619773`、`0000664462`，SAX/4CH 已识别为可用角色，`0000664462` 不再因乱码目录失败；`python -m unittest tests.test_dicom_split_sax_import tests.test_feedback_agent tests.test_codex_feedback_runner` 通过 22 项；`frontend npm run build` 通过。
- 当前边界：未批量重写生产 CVI study / LabelSystem catalog 数据库，未重启服务、未部署；已导入过的旧病例需要在新代码生效后点“重新打开”重导入，或另行执行受限批量索引重建。
- 推测：85 个未匹配登记号可能位于其他数据根、旧传输缺失或登记号格式/重号映射不一致；需要下一轮只读核对后再决定是否补数据 link。


## 2026-08-08 13:58:00 +0800 昆医勾画任务病例受控 symlink 补链与权限处理

- 任务：按用户明确授权，建立受控 symlink 目录，只 link Excel 任务内缺失但已找到原始 DICOM 候选的病例；同时为胡力水开通所有医院权限，并检查杨楠省医院权限问题。
- 已确认：受控 symlink 目录为 `/home/Larry/data/CMR_ALL_excel_task_symlinks`；已创建 79 个病例 wrapper 目录，每例目录内仅有 `DICOM -> 原始DICOM目录` symlink，覆盖 77 个登记号，全部写入 `cvi_case_catalog` 且 `has_dicom=1`；未复制 DICOM，未触发全量 CMR_ALL refresh。
- 已修改：工作站 CMR_ALL 配置支持 `additional_paths`，后续服务重载后会把该受控目录纳入 CMR_ALL 扫描；catalog 摘要支持病例根目录直接放 DICOM 的旧导出结构。
- 权限：胡力水已获得 8 个非系统共享医院库整库权限；杨楠当前已有 `CMR_SCS` 整库权限，后端模拟确认可看到省医院整库入口并访问样例病例。
- 未解决：仍有 8 个登记号缺少可用原始 DICOM 源，其中 2 个未找到候选目录，6 个只找到 NPY/导出图/派生结果。
- 备份与验证：写库前已备份 LabelSystem SQLite 数据库并通过完整性检查；后端 43 个相关测试通过，前端生产 build 通过。
- 推测：杨楠此前检索省医院失败更可能是未切换到省医院整库数据源、浏览器缓存/旧会话或当时权限尚未生效，不像当前权限表缺失。


## 2026-08-10 14:20:00 +0800 昆医 4 例短轴默认只显示 3 层面问题

- 任务：检查 `0000735306`、`0000025377`、`0001286400`、`0002074416` 短轴图像显示不全、只有 3 个层面的问题。
- 已确认：4 例源目录均存在更完整的短轴 stack，不是源 DICOM 只有 3 层；CVI 已导入 study 中同时存在 3-slice `cine_sax` scout 和更完整的 `cine_sax` stack。
- 已确认：当前问题根因是详情接口和部分后端默认逻辑按 series 插入顺序取第一个 `cine_sax`；这 4 例里 3-slice scout 排在完整 stack 前面。
- 已修改：`fetch_study_detail()` 返回 series 时对 `cine_sax` 优先按 `slice_count`、`file_count` 降序排序；自动相位识别引用 SAX 时也优先完整 stack。
- 已验证：修复后本地读取现有 CVI DB，4 例第一个 `cine_sax` 均变为完整 stack：前三例 13 层，`0002074416` 为 13 层；相关后端测试 44 项通过。
- 数据风险：`0000735306`、`0000025377` 的 3-slice 序列已有人工 function 轮廓，未静默改 role 或迁移标注；若要把旧标注隐藏/迁移到完整 stack，需要单独确认。
- 推测：医生/实习生看到“只有 3 层”的直接原因是前端默认选中了 3-slice scout，而不是 DICOM 缺失。


## 2026-08-10 18:08:00 +0800 4 例不完整 SAX 隐藏与服务重载

- 任务：按用户确认，重载服务生效，并废弃/隐藏旧 3-slice 标注入口，让实习生在完整 SAX stack 上重画。
- 已备份：`.cvi-workspace/cvi.db` 写入前备份到 `/home/Larry/backups/LabelSystem/cvi_sax_hide_incomplete/cvi-pre-hide-incomplete-sax-20260810T100451.sqlite3`，完整性检查为 `ok`。
- 已修改 CVI DB：对 `0000735306`、`0000025377`、`0001286400`、`0002074416` 中低于该 study 最大短轴层数的 `cine_sax` 序列改为 `unknown`；轮廓和测量记录未删除。
- 隐藏序列：`3821`、`3799`、`3949`、`3955` 四个 3-slice SAX，以及 `0002074416` 的 6-slice `3956`，避免不完整短轴继续作为功能短轴候选。
- 当前默认完整 stack：`0000735306 -> 3822/SAX_0/13层`，`0000025377 -> 3800/SAX_0/13层`，`0001286400 -> 3950/SAX_0/13层`，`0002074416 -> 3957/SAX_1/13层`。
- 已重载：Flask 后端与 CVI API 已以独立后台进程重新拉起；健康检查 `/api/auth/me` 与 CVI `/gpu/status` 均返回 200。
- 推测：暂无。


## 2026-08-11 12:06:14 +0800 实习资料库历史回填与简历素材整理

- 任务：根据 `zian_workspace/AGENTS.md`、本工作日志、EHR 子项目规则和现有非敏感交付文档，把 6 月至 8 月的实习工作回填到本机入职前建立的实习资料库，形成可持续维护的工作记录、简历素材和面试回答。
- 已确认：本次将实习主线整理为 CMR / ETA 医学影像工作站与肺癌 EHR 离线治理两部分；按“真实任务、我的动作、验证结果、证据位置、未完成边界”组织，不把推测、论文意向或原型能力写成已完成成果。
- 已确认：本机资料库已更新项目总览、项目概况、时间线、待确认问题、历史回填日志、成果证据库、中英文实习描述和面试问答库。
- 已确认：EHR 200 例 L1 / L2 批次当前状态为完成，共 200 例、2,338 份 PDF、12,578 页；1,958 份为 `manual_review_required`，380 份为 `quarantined`。本次只使用匿名聚合状态，不读取或记录病例正文。
- 边界：未写入患者信息、DICOM / EHR 正文、数据库正文、日志正文、token、密钥、账号或远程连接凭据；未修改项目代码、服务、生产数据或权限；未执行 Git 提交或推送。
- 下一步：以后每周至少从远端工作日志向本机实习资料库回填一次；简历只使用已有证据且可解释的条目，曲率、左房和 EHR L3 等仍需把“已开发 / 原型”与“医生验收 / 科研可用”分开。
- 推测：暂无。
## 2026-08-11 14:15:32 +0800 LGE 阈值预览与心肌边界 P0 修复

- 任务：根据师姐对 LGE Scar 越过 endo/epi 心肌环、病灶识别与科研指标可信度的反馈，优先修复当前帧一致性和硬边界问题。
- 已确认：原 LGE 阈值预览与整序列重算固定读取 phase 0，而工作站可显示 Phase 2/3；红色预览因此可能使用不同于当前画面的影像帧。
- 已修改：LGE 预览和重算请求携带当前 `phase_index`；后端使用当前 phase 图像，并在缺少同 phase 轮廓时显式记录采用的 phase 0 轮廓回退；界面预览文字显示实际 P 序号。
- 已修改：Scar、Grey Zone、MVO 在形态学处理后再次裁剪到 `epi - endo - exclude` 有效心肌；Grey Zone 与最终 Scar 保持互斥，MVO 不再统计 exclude 重叠区域。
- 验证：LGE 专项测试 7/7、全部 measurement 测试 52/52、Python 编译、前端补丁本地 Node 语法检查和 `git diff --check` 通过。
- 边界：未修改 n-SD/FWHM 医学公式、remote 缺失 fallback、enhanced 种子语义或科研指标定义；未读取病例/DICOM 像素、数据库正文或日志；未重启服务、部署、提交或推送，线上后端尚未加载本次源码。
- 推测：完成服务重载后应优先用原截图对应的多 phase LGE 序列复测 Phase 切换、红色 overlay 边界和当前层面积，再决定 P1 的 remote/enhanced 临床规则。


## 2026-08-11 14:36:04 +0800 LGE P0 CVI API 重载

- 用户确认：刚才查看当前实现看起来没有明显问题，并明确授权重载服务。
- 已重载：仅重载 `127.0.0.1:8010` 的 CVI API；5000 端口主 Flask 服务、数据库和病例数据未改动。
- 已验证：新 CVI API 进程正常监听 8010，`/gpu/status` 与 `/openapi.json` 均返回 HTTP 200。
- 待人工验收：继续在原多 Phase LGE 序列中检查 Phase 切换、Scar 是否严格位于 endo/epi/exclude 有效心肌内，以及当前层面积是否一致。


## 2026-08-11 17:01:09 +0800 Tissue LGE 主序列人工指定

- 任务：Series Overview 中多个不同方向 LGE 被统一标为 `lge_sax`，且 Tissue LGE 默认取第一个同角色序列；本轮先实现人工、持久化指定实际进入 Tissue LGE 标注的序列，自动方向识别另行处理。
- 已确认：冻结前端运行时按 API 顺序取第一个 `lge_sax`，点击 Series Overview 卡片不会把具体 series 持久化为 Tissue LGE 当前序列；因此同一 Study 有多个 `lge_sax` 时可能默认进入错误序列。
- 已修改：新增 `POST /series/{series_id}/tissue-lge-primary`；只允许选择 `lge_sax`，在该 series 的既有 `metadata_json` 保存 `tissue_lge_primary=true`，并原子清除同一 Study 其他 series 的该标记，不修改角色、DICOM、轮廓或测量。
- 已修改：Study 详情新增 `is_tissue_lge_primary`，并把已指定的 `lge_sax` 排在其他 `lge_sax` 前；现有 Tissue LGE 的“取第一个 `lge_sax`”逻辑因此会使用人工指定序列。若后续把主序列改成非 `lge_sax`，主标记会同步清除。
- 已修改：Series Overview 的 `lge_sax` 卡片增加“设为 Tissue LGE”按钮；当前主序列显示 `Tissue LGE ✓`。设置成功后刷新页面，重新读取持久化顺序。新接口沿用 series 对象级权限代理，未分配账号不能借此修改私有病例。
- 验证：隔离数据库完成“设置第二个 LGE -> 接口返回 -> Study 详情排序/主标记”全链路；CVI 相关回归 21 项通过，私有病例权限回归 19 项通过，前端 typecheck、10 项测试、Vite production build、Python/Node 语法检查及 `git diff --check` 通过。
- 当前边界：未读取病例、DICOM、数据库或日志正文；未修改生产数据库，未重启服务、部署、提交或推送，线上尚未加载本次功能。LGE 2CH/3CH/4CH 与 SAX 的自动识别和更细角色模型尚未在本轮实现。
- 推测：后续自动识别需要把序列描述/ProtocolName 与 ImageOrientationPatient 联合起来；仅靠当前文本中的 `LGE`、`2CH`、`4CH` 标记不足以稳定覆盖截图中的通用命名序列。


## 2026-08-11 17:09:56 +0800 Tissue LGE 主序列功能上线

- 用户确认：本轮先只上线 Tissue LGE 人工主序列选择供验收；SAX、4CH 等其他模块也需要同类自选能力，但留待后续统一设计和实现。
- 上线前备份：线上 `apps/web/dist/index.html` 与 `index-CVIEmbeddedPatch.js` 已备份到 `/home/Larry/backups/LabelSystem/tissue_lge_primary_20260811T1705/`。
- 已安装：线上 frozen runtime 已同步为源码版本 `v2026.08.11-2`，入口 cache-buster 更新为 `20260811-tissue-lge-primary-1`；源码与线上补丁 SHA-256 一致。
- 已重载：仅重载 `127.0.0.1:8010` 的 CVI API，主 Flask 5000 服务未重启；新进程 PID `2141116`，由 PID 1 托管。
- 线上验证：`/gpu/status` 返回 200；OpenAPI 已包含 `/series/{series_id}/tissue-lge-primary` 和 `SeriesSummary.is_tissue_lge_primary`；用不存在的 series ID 验证新接口返回 404，未写入病例数据；线上前端文件已包含“设为 Tissue LGE”与 `Tissue LGE ✓`。
- 当前边界：未替用户选择任何生产病例的主 LGE，未读取病例、DICOM、数据库或日志正文；SAX、4CH 及更通用的模块序列自选功能尚未实现。
- 推测：暂无。


## 2026-08-11 17:23:33 +0800 Tissue LGE 设置后 Series Overview 标签消失修复

- 用户反馈：Tissue LGE 主序列可以成功更改，但设置后页面刷新，Series Overview 中 SAX/2CH/3CH/4CH/LGE/清除等人工角色标签全部消失；截图版本徽标回退为 `v2026.07.16-4`。
- 已确认：服务器源码和线上文件已是新版本，但浏览器刷新后命中了旧 `index.html` 与更早的同名补丁缓存，形成入口/运行时版本混用；主 LGE 后端标记没有因标签消失被清除。
- 已修改：工作站 `index.html` 和 `assets/index-CVIEmbeddedPatch.js` 响应增加 `no-store, no-cache, must-revalidate, max-age=0`；主 LGE 设置成功后不再使用普通 `reload()`，改为携带 `cviRuntime` 版本参数的 `location.replace()`，强制读取同一版本入口。
- 已安装：前端运行时升级为 `v2026.08.11-3`，cache-buster 为 `20260811-tissue-lge-primary-2`；上线前文件备份位于 `/home/Larry/backups/LabelSystem/tissue_lge_cache_fix_20260811T1720/`。
- 已重载：仅为缓存响应头生效而重载主 Flask 5000 服务，新 PID `2183509`；CVI API 8010 保持 PID `2141116`。`/api/auth/me` 与 `/gpu/status` 均返回 200。
- 验证：私有病例/静态缓存相关测试 20 项通过；前端 typecheck、10 项测试、production build、Python/Node 语法检查及 `git diff --check` 通过；测试覆盖工作站 fallback index 与补丁资源均返回 no-store。
- 验收提示：用户当前浏览器已持有旧文档，需要执行一次 `Ctrl+F5`；看到版本徽标 `v2026.08.11-3` 后，Series Overview 人工标签应恢复，后续设置主 LGE 不再回退旧缓存。
- 推测：暂无。


## 2026-08-12 09:26:19 +0800 病例搜索与打开性能只读审计

- 任务：只读梳理病例搜索、点击打开、已有 Study 复用、工作站首屏与图像加载链路，寻找当前“打开病例特别慢”的优化空间。
- 已确认：已有病例每次打开仍先解析目录并递归统计 DICOM，再通过完整 `/studies/{id}` 详情验证 Study；iframe 随后又请求一次同样的 Study 详情，存在明显重复工作。
- 已确认：Study 详情按 Series 逐条查询 frames，属于 N+1 查询，并把所有 frame 元数据一次性返回；Series Overview 又为大量 Series 请求缩略图，冷缓存时逐张读取 DICOM 像素、窗宽归一化、PNG 编码并加用户水印。
- 已确认：病例列表虽只展示最多 300 条，后端当前先 `.all()` 读取全部匹配记录再在 Python 中截断；`%关键词%` 模糊搜索不利于普通索引，病例权限可能逐条查询，列表还同步聚合六类旧评估表与 CVI 标注状态。
- 已确认：CVI SQLite schema 缺少若干常用查询索引，例如 `series.study_id`、frames 的 `(series_id, slice_index, phase_index)`、contours/measurements 的 `series_id`；LabelSystem 病例目录与权限列主要依赖唯一约束，缺少针对列表/权限查询形态的显式复合索引。
- 优先建议：P0 先建立耗时分段；P1 跳过已导入病例的目录复扫和完整详情验证、复用验证改轻量 endpoint、列表查询在 SQL 层 limit、批量权限过滤；P1 再把 Study 首屏改为概要/按需 frames、批量查询 frames；P2 对缩略图做 lazy/虚拟化与预生成，对搜索做规范化检索列或 FTS。
- 当前边界：未读取病例、DICOM、数据库或日志正文；未修改代码、数据库、服务或生产数据。实际各阶段秒数仍需通过轻量计时埋点或浏览器 Network 验证。
- 推测：大文件病例首次打开的主要耗时更可能来自病例目录统计和 Series Overview 冷缩略图生成；已打开过的病例若仍慢，则重复 Study payload、权限/标注聚合和浏览器并发请求影响会更突出。


## 2026-08-12 11:10:00 +0800 EHR GPU 有界队列与第二批安全调度

- 任务：建立 CPU 渲染生产者、有界 OCR 队列、单常驻 GPU 消费者、有界本地脱敏后处理队列，并在准确性门禁通过后准备第二个 984 例批次。
- 已确认：最终分层门禁覆盖 12 份、33 页匿名文档；CPU 路由 10 份、CUDA 路由 2 份，文字、数字 token、bbox、隐私状态及逐页行数均与既有 CPU 基线 100% 一致，外部 API 调用 0。
- 已实现：GPU provider 三模型 fail-closed；短会诊文档使用风险路由 GPU，其他类别保持常驻 CPU；数字、低置信度和短字段执行 CPU 复核；加入 SIGTERM 断点恢复、staging 清理、两级队列指标和本地脱敏消费者。
- 已实现：批次中心展示 GPU 模式、门禁状态、GPU 编号、CPU 生产者、OCR/脱敏队列、GPU 消费者、GPU 直出与 CPU 数字复核；后端在门禁未通过时拒绝正式 GPU 批次启动。
- 当前状态：第二批仍未开始 OCR，已进入 `waiting_gpu`；GPU 0 存在无法映射到活动系统进程的 compute 占用条目，安全调度器最长等待 12 小时，连续确认空闲后才自动启动，不抢占未知任务。
- 已验证：GPU/Dashboard 定向回归通过；Dashboard 已以独立会话恢复并通过延迟健康检查；第一批 CPU OCR、8765 服务与原始 PDF 均未修改或停止。
- 推测：GPU 0 的 0MB compute 条目可能是驱动层残留或短任务尚未清理；在 `nvidia-smi` 明确移除前继续按占用处理。


## 2026-08-12 11:18:00 +0800 第二批获准共享 GPU 0 启动

- 用户明确授权：GPU 0 即使存在组内其他任务也可共享使用；有问题由用户协调。本授权只应用于第二批，不改变后续批次默认空闲 GPU 门禁。
- 已实现：GPU 启动器新增显式 `--allow-active-gpu`，默认仍检查空闲；CUDA 三模型首选 provider、禁止静默回退 CPU、连续失败停止、24小时、100GB磁盘保护和断点恢复均保留。
- 当前状态：第二批 984 例、13,781 份 PDF 已启动，正在执行全批页数预检；预检阶段不加载模型、不占用 GPU，完成后自动进入4个CPU渲染生产者、容量3 OCR队列、单常驻GPU消费者、容量2本地脱敏队列。
- 可视化：Dashboard 已恢复并通过延迟健康检查；批次中心会先显示预检中，state 落盘后显示 producer、GPU consumer、脱敏 worker、队列深度和 CPU 复核聚合指标。
- 边界：第一批 CPU OCR、8765 服务、原始 PDF、既有输出和外部 API 均未修改或停止。
- 推测：预检耗时取决于 13,781 次本地 PDF 元数据读取，GPU 显存暂不增长属于预期。


## 2026-08-12 16:30:00 +0800 CMR 卡顿、仁济空列表与 AI 专家只读诊断

- 任务：只读判断当日 CMR 工作站严重卡顿是否与 EHR 数据治理有关，并分别排查上海仁济医院-HCM 病例列表为 0 与 AI 专家不可用；未重启服务、停止任务或读取病例、数据库及日志正文。
- 已确认：服务器共有 112 个逻辑 CPU，现场 load average 为 295.90/352.50/342.31；连续采样 CPU 使用率 95%–100%、空闲 0%，有 300 余个 runnable 进程。内存仍有约 398 GiB 可用，数据盘剩余约 17 TiB，因此主要瓶颈是 CPU 调度争用，不是内存或磁盘容量耗尽。
- 已确认：EHR 数据治理同时运行第一批 8-worker RapidOCR 与多路 GPU hybrid 批次；现场多个 RapidOCR 子进程各自使用约 4–18 个 CPU 核，治理任务是当前 CPU 满载的重要来源。服务器上同时存在其他用户的模型训练和数据 worker，卡顿并非治理任务单独造成，但与其并发强相关。
- 已确认：LabelSystem 5000 与 CVI API 8010 进程仍存活并监听，本机轻量健康请求分别约 0.04 秒和 0.002 秒；说明主服务未整体宕机，但重型病例扫描、缩略图与 Study 请求仍会在满载 CPU 下排队。
- 已确认：仁济 HCM 配置目录存在、可读，顶层约 297 个病例目录；浅层目录枚举约 0.04 秒，不能归因于源目录丢失。病例接口在目录索引为空时会触发同步刷新；若刷新锁已被其他请求占用，会设置 `refresh_in_progress=true` 并先返回当前空结果。当前前端未消费或重试该状态，因此可直接展示“0 条”，CPU 满载会放大该竞争窗口。
- 已确认：AI 专家网关配置文件存在且 API 地址、密钥已配置，但 `enabled=false`；反馈接口会保存用户消息后直接进入“AI 专家暂时不可用”的兜底分支。公网网关端点可达并能返回 HTTP 401，故当前直接原因是功能被配置禁用，不是 CMR CPU 卡顿导致的调用超时。
- 推测：用户截图中的仁济 0 条大概率是病例目录刷新锁竞争或刷新尚未完成导致的瞬时空列表；因为未使用用户登录态读取具体 API 响应、未读数据库和日志正文，本轮尚不能确认当次请求是 `refresh_in_progress`、权限过滤还是索引内容异常。


## 2026-08-12 16:49:00 +0800 AI 专家根因纠正与 OCR 不停机方案

- 用户补充：OCR 批次可能无法停止，并追问 AI 专家关闭原因以及能否读取其 Chrome 登录态。
- 纠正：上一轮用简单文本匹配配置文件时受 JSON 空格格式影响，误判 `enabled=false`；本轮通过结构化 JSON 读取确认实际为 `enabled=true`，不存在人工关闭。配置文件修改时间为 2026-07-23 11:35:28，旧健康检查在当日成功，模型为 `[j]gpt-5.5`。
- 已确认：实时 `/v1/models` 请求使用当前凭据返回 HTTP 200，配置模型存在；但实时 `/v1/chat/completions` 返回 HTTP 401 `Invalid token`。因此 AI 专家今天不可用的直接原因是网关生成接口不再接受当前令牌，可能为网关侧令牌失效或撤销，不是开关关闭，也不是 CMR CPU 卡顿。
- 已确认：Chrome 当前未开放 remote debugging，故无法接入用户现有登录标签页；用户在 `chrome://inspect/#remote-debugging` 勾选 `Allow remote debugging for this browser instance` 后，可只读读取登录态下仁济病例列表 API 的状态码、`refresh_in_progress` 和返回计数，无需提供账号密码。
- 已确认：16:50 服务器 load average 已升至 452.50/446.92/411.12，连续采样 CPU 使用率 96%–100%、空闲 0%；OCR 主进程和子进程当前 nice 均为 0。
- 建议：不停止 OCR，优先把治理进程树整体调整为较低 CPU 调度优先级（例如 nice +10），使其继续运行并保留断点、输出和任务状态，但在 CMR 请求到来时让出 CPU；该操作会降低治理吞吐量，需用户明确确认后执行。
- 推测：AI 网关对 models 与 chat 接口可能采用不同鉴权或上游路由；需要管理员更新可用于生成接口的令牌后再做健康检查。仁济 0 条仍需 Chrome 登录态接口证据才能在刷新竞争、权限过滤和索引异常之间定论。


## 2026-08-12 17:07:00 +0800 Chrome 登录态、工作站密钥与负载归属复核

- 用户授权并完成 Chrome remote debugging；本轮仅通过现有登录态读取接口状态和网关控制台状态，不读取病例条目、患者内容或完整密钥。
- 已确认：服务器实际加载的工作站密钥以 `sk-d5D` 开头、以 `IAcd` 结尾、长度 51；与网关“CMR Workstation”已启用密钥的可见首尾完全一致，配置文件自 2026-07-23 11:35 后未改动。
- 已确认：网关使用日志明确记录“CMR Workstation / `[j]gpt-5.5` / status_code=401 / Invalid token”；同一工作站密钥访问模型列表返回 200 且能看到该模型。因此工作站密钥本身仍有效，故障位于 `[j]gpt-5.5` 对应上游生成通道或其供应商令牌，而不是工作站密钥开关。
- 已确认：使用用户现有 Chrome 登录态请求仁济 HCM 病例列表返回 HTTP 200、297/297 条、约 765 ms，`refresh_in_progress=false`，数据源选项存在；当前权限、索引和目录均正常。此前 0 条为瞬时问题，与目录丢失或持续权限故障不符。
- 已确认：112 核服务器当时 load average 约 341/376/392，CPU 连续采样空闲 0%；3 秒瞬时采样中，第一批 RapidOCR 约使用 66 核，4 路 GPU hybrid 的 CPU 辅助约 2.6 核，合计约 68 核。RapidOCR 8 个 worker 合计约 1610 个线程，其中约 278 个处于 runnable，存在显著线程过度订阅。
- 已确认：组内其他主要 CPU 负载包括 wangshuyang 的 3 个模型 PPL 评估进程（瞬时约 9 核，其中 Llama 任务约 8.8 核），以及 wanghongfei 的医学分割训练/DataLoader（瞬时约 5 核、约 27 个活跃相关进程）。GPU 1/3 主要被其训练/PPL 占用；GPU 0 为 4 路 EHR hybrid，总显存约 9.3 GiB，采样时 GPU 利用率接近 0，主要瓶颈仍在第一批 CPU RapidOCR。
- 可协调项：不应停止 LabelSystem 5000、CVI API 8010、治理 Dashboard 或未经确认的组内训练；可以优先与 wangshuyang 确认是否暂停 3 个 PPL 评估，与 wanghongfei 确认是否降低医学分割 DataLoader workers。用户侧最有效且无需停批次的措施是限制第一批 RapidOCR 内部线程数或降低其调度优先级；单纯关闭其他人的任务不足以根治 1610 线程过度订阅。
- 其他发现：Larry 账号下有 2 个 `mads_response_test6.py` 测试进程、一个长时间 `grep -RInE 1.4Beta` 和若干短时校验进程，但瞬时 CPU 占比远低于 RapidOCR；是否结束测试进程需先确认其所属实验，长时间 grep 可作为低风险清理候选但不是主要矛盾。
- 推测：仁济此前空列表由同步索引刷新锁竞争造成的概率较高；AI 专家恢复需要网关管理员修复 `[j]gpt-5.5` 上游通道令牌或切换到支持当前 Chat Completions 调用方式的健康模型。

## 2026-08-12 17:48:00 +0800 OCR 保活与 CMR 资源让渡

- 用户目标：OCR 必须持续运行，但不能因多个全量分组并发把 CMR 工作站压垮；获准保留一至两个必要进程并暂停重复批次。
- 已执行：保留 `batch-full-01` CPU 全量与 `batch-full-02` GPU 全量两条流水线；`batch-full-03/04/05` 通过 SIGTERM 正常暂停并落盘为 `paused / signal_15`，既有输出未删除，可断点恢复。
- 已执行：`batch-full-01` 从 8 worker 收敛为 2 worker，并限制在 24 个逻辑 CPU、nice 15；所有 RapidOCR 内部线程均继承/被持续校正到相同 CPU affinity 与低优先级。
- 已执行：部署单实例监督器，每 10 秒维持 CPU 与 nice 限制；当前批次达到 24 小时保护退出或意外退出后，只要尚未完成，就会自动从 state 以相同的 2-worker 配置续跑，完成后监督器自动退出。
- 已确认：调整前服务器 load average 曾约 350–450、CPU 空闲 0%；调整后最近降至约 139，短采样仍受组内模型评估与医学分割训练影响，但 CMR 5000 与 CVI 8010 本机轻量请求保持约 2–4 ms。
- 已确认：两条保留流水线均持续推进；CPU 全量的待处理文档继续下降，GPU 全量的人工复核/隔离计数继续增加。状态文件中的旧 `stop_reason=runtime_limit` 是上一轮 24 小时退出遗留字段，不代表当前进程已停止。
- 边界：未停止 LabelSystem 5000、CVI API 8010 或治理 Dashboard；未删除 OCR 输出、原始 PDF 或病例数据；未停止其他用户的训练/评估进程。
- 推测：当前剩余高负载主要来自保留的 24 核 CPU OCR，以及组内同时运行的 PPL 评估和多个医学分割 DataLoader。若 CMR 实际操作仍卡顿，可继续把 CPU OCR 缩到 16 核或 1 worker，但应先以医生端病例打开与搜索实测决定。


## 2026-08-13 Tissue LGE 验收异常与 CMR 数据索引关系复盘

- 任务：根据用户验收截图追踪 Tissue LGE 主序列功能改动，诊断 Series Overview 中按钮范围、主序列标识、Slice / Phase Matrix 和重新打开病例后的状态问题；同时阅读 CMR Data Platform 当前状态，梳理其与标注工作的关系。
- 已确认：每张 Overview 卡片都出现“设为 Tissue LGE”是前端 CSS 冲突。脚本已对非 `lge_sax` 按钮设置 `hidden=true`，但 `.cvi-role-choice { display: inline-flex; }` 覆盖了浏览器的 `[hidden]` 隐藏规则，导致 cine、unknown 等非 LGE 卡片也错误显示按钮。
- 已确认：已选中的主 LGE 原本应由接口字段 `is_tissue_lge_primary` 回填为 `Tissue LGE ✓`；当前病例重新打开后两个 LGE 均为未选中状态，不是单纯文案未更新，而是主标记已经在强制重导入过程中丢失。
- 已确认：左侧当前病例的“重新打开”会传入 `force=true`，后端因此重新执行 Study import；导入器删除旧 Study 后创建新 Study，只迁移轮廓、测量和报告，不迁移 Series 人工角色、`metadata_json` 与 `tissue_lge_primary`。截图中的 Study ID 从 `000734` 变为 `000735` 与该调用链一致。
- 已确认：Slice / Phase Matrix 是既有冻结运行时中的层×相位导航和批量操作组件，不是 Tissue LGE 功能新加入的组件；Series Overview 本应隐藏它。现有补丁依赖 `.protocol-item.is-active` 和 DOM 文本判断当前页面，截图中页面已进入 Overview 但补丁仍识别为 Cine SAX Workspace，说明隐藏判断不可靠。
- 建议修复：为 `[hidden]` 增加明确 CSS 规则并以 JS 显式显示/隐藏双保险；Overview 可见时通过稳定页面状态或根节点 data 属性强制隐藏 Navigator/Matrix；普通“重新打开”只复用既有 Study 和重载 iframe，不再等同于强制重建索引；真正重导入作为独立受控操作，并按 Series UID 安全迁移人工角色和 Tissue 主标记。
- 测试缺口：现有前端 contract 测试只验证补丁字符串存在，尚未覆盖非 LGE 按钮隐藏、唯一主 LGE 勾选、切换主序列、Overview 隐藏 Matrix、普通重新打开保持 Study ID，以及真正重导入保留人工状态。
- 已确认：`/home/Larry/data/CMR_Data_Platform` 是服务器私有 CMR 数据的统一只读目录索引。它用 DICOM header-only 扫描与只读适配器登记多院数据源，按 UID 聚合 Study/Series，统一识别 cine SAX、2CH/3CH/4CH、LGE、mapping、perfusion、flow，并索引报告、分类、标签、轮廓/分割的存在性与 provenance，不复制敏感正文或修改原数据。
- 已确认：该平台可提供数据源扫描状态、序列覆盖、缺报告/分类/分割清单、标签来源、HCM 治理队列和跨来源 UID 去重口径；当前四川整理批次为 518 个 Study UID、6002 个唯一 Series，503/518 同时具备 4CH、SAX、LGE。LabelSystem 侧登记 9815 个 canonical external cases 与 7134 条标签事实。
- 与当前工作的关系：数据平台解决“全服务器有哪些可用病例、完整度和来源如何”的数据治理与课题队列问题；Tissue LGE 选择解决“进入某个病例后具体用哪条 LGE 做标注”的单病例工作流问题。两者互补，但平台目前不会控制 CVI 的具体主 LGE，也不应直接反向写工作站；后续可把工作站确认的主序列作为带 provenance 的只读事实同步进平台。
- 当前边界：本轮只读调查并追加工作记录；未修改 Tissue LGE 业务代码、未部署、未重启服务、未改生产数据库，也未读取病例、DICOM、数据库或日志正文。
- 推测：暂无。

## 2026-08-13 13:27:00 +0800 MRIAgent demo 交互不可点击修复

- 任务：处理 `http://47.108.84.221:8282/demo` 中 Start Diagnosis Simulation、Patient 1/2/3 切换、CMR 详情等按钮“看得到但点不动”的问题，并按用户授权重载生效。
- 已确认：公网 HTML 已更新且包含 `data-demo-start`、`data-demo-patient`、节点解释面板和 DOM fallback；实际引用的 Next 静态资源最终均返回 200，`/_next/static/chunks/app/demo/page.js` 下载体积约 956KB。
- 已确认：问题核心不是 DICOM 或病例数据路径，而是前端交互兜底逻辑过早因 `__MRIAGENT_REACT_READY` 退出；当 React 事件链异常或未完全接管时，原生 fallback 没有继续处理点击，导致页面只能显示、不能交互。
- 已执行：在 `CardioAgentWeb/src/components/demo/DemoDashboard.tsx` 增加 React handler 注册；原生捕获点击现在始终接管 `data-demo-patient`、`data-demo-start`、`data-demo-details`，React 可用时调用 React 状态，不可用时走 DOM fallback。
- 已执行：重启 MRIAgent Next demo 服务；Flask `backend/app.py` 保持运行。公网 `/demo` 最终抓取确认包含 `__MRIAGENT_DEMO_REACT_HANDLERS__`，且旧的 `if (reactReady()) return` 已不存在。
- 验证：`npm run lint` 0 error、10 warning；`npm run build` 通过。最终公网主 JS 为 HTTP 200、`application/javascript`、955888 bytes。
- 边界：未读取或修改 DICOM 内容、病例正文、数据库、密钥或日志正文；未复制数据、未全量扫盘。
- 推测：如果用户浏览器仍显示旧行为，最可能是浏览器/代理缓存或 DevTools 保留了旧页面实例；新开无痕窗口或清空该站点缓存应能确认。

## 2026-08-13 13:52:00 +0800 MRIAgent demo 生产模式与降级提示修正

- 任务：用户确认无痕窗口可交互，但普通页面仍点不动；同时反馈当前展示过于简单。
- 已确认：截图中的“点击节点触发的兜底详情”说明页面进入 DOM fallback 降级模式，而不是完整 React 交互模式。该模式只用于防止页面完全不可点，展示内容确实简化，不应作为正式 demo 效果。
- 已确认：无痕窗口可用而普通页不可用，符合旧标签页/站点缓存保留旧 JS runtime 的表现；服务端当前已经提供新版本。
- 已执行：fallback 改为“先等待 React handler 接管，最多 6 秒；只有 React 真未接管时才降级”，并在页面顶部显示 `交互引擎加载中` 或 `React 交互未接管，已进入降级演示模式`，方便验收区分正式模式和兜底模式。
- 已执行：Showcase 从 `next dev` 切换到生产 `next start -p 3015`。公网 `/demo` 当前不再引用 dev 路径 `/_next/static/chunks/app/demo/page.js`，而是生产拆分 chunk；实际公网 10 个 JS chunk 全部 200，最大约 228KB。
- 已执行：`start_background.sh` 已同步改为启动 `npm run start -- -p 3015`，避免下次整体重启又回到 dev 模式。
- 验证：`npm run lint` 0 error、10 warning；`npm run build` 通过；公网 HTML 含 `data-demo-hydration-status`，生产 chunk 含 `__MRIAGENT_DEMO_REACT_HANDLERS__`、加载中和降级提示文案。
- 推测：若后续仍频繁进入降级模式，需要继续查浏览器 Console 的真实 React hydration/runtime 错误；展示内容本身还需要产品化重做，不能靠 fallback 简化页交付。

## 2026-08-13 14:33:00 +0800 MRIAgent demo 视觉层级、画布拖拽与报告高亮

- 任务：根据用户截图继续优化 MRIAgent demo，重点处理字号/颜色层级差、流程图放大后移动不方便、Generated Report 文本单调的问题。
- 已执行：参考高质量暗色产品站点和中国传统色板思路，采用深靛背景、天青/青绿主强调、紫色辅助、琥珀警示、玫瑰异常的克制医疗科技配色；全局滚动条和 glass-card 质感同步调整。
- 已执行：流程图节点卡片加宽、字号提高、agent 标签和指标值增强对比；选中节点使用 cyan ring 和柔和发光，warning 节点使用 amber，异常/正常状态色更明确。
- 已执行：流程图画布改为隐藏原生滚动条的网格画布；按住空白区域可拖拽平移，缩放按钮保留，点击 100% 会同时重置缩放和平移。
- 已执行：Generated Report 从纯 `pre` 改为结构化富文本渲染；章节编号、标题、项目符号、关键指标数值、LGE、正常/异常、心肌梗死/心肌炎等诊断关键词会高亮显示。
- 验证：`npm run lint` 0 error、10 warning；`npm run build` 通过；已重启 `next start -p 3015`。公网 `/demo` HTML 包含拖拽提示、新背景和 hydration 标记；所有公网 Next CSS/JS 资源均为 200，CSS 已包含 `cursor-grab`、自定义 scrollbar、cyan/rose 高亮样式。
- 边界：仅修改 MRIAgent demo 前端样式和交互逻辑；未读取或修改 DICOM、病例正文、数据库、密钥或后端数据。
- 推测：这轮主要解决“看起来专业一点、用起来顺手一点”；如果要达到正式展示级别，下一步应继续把每个节点和真实图像/分割结果/指标解释/报告片段绑定得更强。

## 2026-08-13 15:12:10 +0800 MRIAgent DICOM 直接生成报告子页面

- 任务：根据师兄反馈，在 `showcase` 下新增与流程 demo 并列的“DICOM to Report”子页面，支持上传/拖拽 DICOM 或 zip，只输出 MRIAgent 生成的 CMR 报告，不展示流程图。
- 已执行：新增 `/showcase/report` 页面入口，并在 Showcase 主页、顶部导航和 LabelSystem overlay 中加入“报告生成 / DICOM to Report”入口；页面支持文件、文件夹、zip 上传，显示任务状态、识别序列、DICOM 文件数、Series 数、选中的 SAX/4CH/LGE series 和最终报告。
- 已执行：LabelSystem 后端新增 `POST /api/mri-report/jobs` 与 `GET /api/mri-report/jobs/<job_id>`；上传数据仅落在 MRIAgent 受控目录 `data/showcase_uploads`，输出仅落在 `src/output/showcase_uploads`，不复制或扫描生产病例库。
- 已执行：上传预处理改为 header-only DICOM 分组：优先读取 `SeriesInstanceUID`、`SeriesDescription`、`ProtocolName`、`ImageType` 等元信息，按 series 选择 SAX/4CH/LGE，并把 `.ima/.dicom/无后缀 DICOM` 在受控目录中统一整理为 `.dcm`，适配 MRIAgent loader。
- 已执行：增加 zip 路径穿越防护、解压后 2GB 限制、非 DICOM 杂文件过滤、后台任务单并发信号量，避免多个上传任务同时抢 MRIAgent/GPU 资源。
- 验证：后端 `python -m py_compile backend/cardio_showcase.py` 通过；Next `npm run lint` 0 error、10 个既有 warning；Next `npm run build` 通过；临时合成 DICOM header 验证可分组为 `SAX/4CH/LGE`；本机 3015、Flask 5000 代理、公网 8282 `/showcase/report` 均返回 200，未登录上传 API 返回 401。
- 边界：未读取真实病例 DICOM、数据库、密钥或日志正文；未用生产病例做完整 MRIAgent 报告跑通测试。真实可用性还需要登录态上传一组非敏感/授权测试 DICOM 验证报告质量与耗时。
- 推测：若真实上传失败，最常见原因会是上传数据缺少完整 SAX/4CH/LGE、医院命名导致序列类型仍识别不准，或 MRIAgent 模型/LLM 环节运行超时；页面状态区现在能先帮助定位是哪类问题。

## 2026-08-13 15:55:38 +0800 MRIAgent 报告页低可信输出拦截与拖拽修复

- 任务：用户真实上传 `data/sca` 下完整病例后发现拖拽只识别 1 个外层文件夹，且页面展示了包含 `sequence_classification/requested_metrics/raw_measurements` 的内部 JSON，不像临床报告。
- 已确认：该次任务识别到了 4CH/LGE/SAX，但自动测量结果低可信：SAX 仅得到 1 个有效时相，ED 与 ES 均为 0，LVEDV 与 LVESV 相同且约 3.7 mL，LVEF/RVEDV/RVEF 为 0；这种结果不应输出诊断结论。
- 已确认：MRIAgent core 的在线报告接口失败后会走 fallback，旧 fallback 直接 `json.dumps(metrics)` 写入“量化指标”，并写“测量结果可信”，导致内部 JSON 被当作报告展示。
- 已执行：修正 MRIAgent core fallback 报告，只输出自动分析摘要、可用指标和质量控制提示，不再泄露 raw metrics，也不再声称测量可信。
- 已执行：LabelSystem showcase 后端新增质量门控：检查 SAX 时相数、ED/ES 是否相同、LVEDV 是否异常过小、LVEDV/LVESV 是否相同、EF 是否超出生理范围、右室是否为 0；低可信时拦截诊断结论，返回“CMR 自动分析质量控制结果”和复核建议。
- 已执行：前端拖拽改为使用浏览器 `webkitGetAsEntry()` 递归读取目录；如果浏览器仍只提供外层目录，会提示用户使用“选文件夹”。
- 验证：后端与 MRIAgent core `py_compile` 通过；Next `npm run lint` 0 error、10 个既有 warning；Next `npm run build` 通过；质量门控单元级验证可拦截包含内部 JSON 的低可信报告；已重启 backend 与 showcase，公网 `/showcase/report` 返回 200。
- 边界：本轮未读取真实 DICOM 内容、数据库、密钥或日志正文；未重新跑用户上传病例。下一步若要定位为什么 `data/sca` 只剩单时相，需要在用户授权下针对该病例做 header-only DICOM 序列/TriggerTime/InstanceNumber 检查。
- 推测：该例真正失败点可能在上传预处理选择的 SAX series 不适合当前 pipeline，或 pipeline 对该数据的 TriggerTime/时相分组策略不适配；MRIAgent 的“真实能力”在历史批处理里能生成较完整报告，但当前 showcase 上传链路还不能等同于稳定临床可用入口。

## 2026-08-13 16:39:32 +0800 昆医二院心梗病例 header-only 上传问题确认

- 任务：用户上传 `/home/Larry/data/CMR_RenJi/心肌梗死200例_extracted/昆医二院/ZHU FENG CHUN_3562317_160314/` 后，报告页只显示 SAX 9 张、低可信、无指标，要求解释并检查。
- 已确认：对该目录做 header-only DICOM 检查，不读取像素内容。完整目录实际包含 1050 个 DICOM、13 个 Series，包含可用于报告流程的 `B-TFE_M2D_SA` cine SAX 540 张、`B-TFE_4CH` 30 张、`PSIR_10min` LGE 27 张，同时还有 Survey、Interactive、2CH/3CH、T2_STIR、IR_TFE 等非目标序列。
- 已确认：用户页面这次实际上传进入后端的只有 9 个 DICOM、1 个 Series，且是 `T2_STIR_BB`。因此不是该完整病例缺 SAX/4CH/LGE，而是浏览器/页面选择时只传了一个 T2/STIR 子序列。
- 已执行：后端序列识别规则收紧，Survey、Interactive、T2/STIR、2CH/3CH、perfusion/DYN、Look-Locker 等不再默认归为 SAX；如果上传数据未同时识别到 SAX、4CH、LGE，则直接拒绝生成报告，并提示缺少哪些序列、当前检测到多少 DICOM/Series 和主要序列名。
- 验证：用完整目录模拟上传可识别 `SAX=540 / 4CH=30 / LGE=27 / detected_series=13`；用 `T2_STIR_BB` 子目录模拟上传会被拒绝并提示缺少 `4CH, LGE, SAX`。后端 `py_compile` 通过，backend 已重启，公网 `/showcase/report` 返回 200。
- 边界：未读取 DICOM 像素、数据库、密钥或日志正文；没有把完整病例重新跑 MRIAgent 报告，只确认上传分组与序列筛选逻辑。
- 推测：用户端这次很可能是在文件选择/拖拽时只选中了 `T2_STIR_BB_161717_701` 子目录，或浏览器没有把完整目录递归传上来。下一次应点击“选文件夹”，选择病例根目录 `ZHU FENG CHUN_3562317_160314`，页面左侧应显示约 1050 个 DICOM、13 个 Series，并识别到 SAX/4CH/LGE 后再生成。

## 2026-08-13 17:13:49 +0800 报告页服务器路径模式与大文件上传结论

- 任务：用户尝试通过公网浏览器上传 1050 个 DICOM、约 134 MB 的完整病例，页面本地显示已选择 1050 个文件，但点击生成后 `Failed to fetch`，不确定是否上传成功。
- 已确认：页面显示“1050 个文件”只代表浏览器本地文件选择成功，不代表已上传到服务器；当前实现是点击“生成报告”后才开始 multipart 上传。公网 API 未登录请求仍返回 401、页面 200，说明服务整体未挂，`Failed to fetch` 更符合公网大 multipart 请求被中断/超时/代理限制。
- 已执行：新增“服务器路径”模式，默认不再要求服务器已有病例通过浏览器上传；后端只允许 `/home/Larry/data` 下病例目录，做 header-only 扫描并在受控 `showcase_uploads` 下建立 symlink，不复制 DICOM。
- 已执行：前端报告页默认显示服务器路径输入框，并提供“填入刚才这例”按钮；外部新病例才切换到“上传文件夹”模式。
- 验证：服务器路径模式对刚才这例可识别 `SAX=540 / 4CH=30 / LGE=27 / detected_series=13`，并确认生成目录中 DICOM 是 symlink；Next lint 0 error、10 个既有 warning，Next build 通过；backend 与 showcase 已重启，公网 `/showcase/report` 返回 200。
- 边界：本轮没有正式跑完整 MRIAgent 诊断，只验证服务器路径输入、受控 symlink 建立和序列识别；真实报告质量仍需下一次用服务器路径模式触发后观察 pipeline 结果。
- 推测：实际应用中，工作站/服务器已有病例应走病例 ID 或服务器路径，不应走公网浏览器上传；上传模式只适合外部临时病例或较小 zip。

## 2026-08-14 14:26:12 +0800 MRIAgent 真实 DICOM 算法验证线

- 任务：按用户要求先做算法验证线，在 `/home/Larry/data` 的不同 CMR 数据源每源抽一例，验证 MRIAgent 是否能从真实完整 DICOM 稳定输出报告。
- 已执行：建立 `zian_workspace/mriagent_validation/`；使用受控 symlink 输入目录 `/home/Larry/code/Ziqiu/MRIAgent/data/validation_algorithm_symlinks`，不复制 DICOM、不全量扫盘；主 LLM 通过空 `LLM_API_KEY` 走离线 fallback，LGE 使用 legacy 模式。
- 已确认：CMR_ALL、Chendu、SCS、YA、RenJi/Kunyi 共 5 个源可准备完整 SAX/4CH/LGE 输入并跑完，均生成 `metrics.json` 和 `report.json`；SCS_2 候选未能同时识别到 SAX/4CH/LGE，先归为输入识别失败。
- 已确认：5/5 跑完病例质量门禁均为 `low`。主要问题集中在 SAX 心功能链路：有效时相为 0 或 1，ED 与 ES 相同或缺失，LVEF/RVEF 为 0 或缺失，部分体积明显不合理。RenJi/Kunyi 用户测试病例复现“完整输入能识别，但 SAX 分割无有效结果”的问题。
- 已确认：LGE 链路也不稳，日志显示 LGE segmentation 使用 SAX model；CMR_ALL 出现过内部序列分类将 LGE 判成 SAX 的情况。
- 产物：详细报告见 `zian_workspace/mriagent_validation/algorithm_validation_report.md`；结构化汇总见 `zian_workspace/mriagent_validation/results/validation_summary.json`。
- 推测：当前 demo 更像挑选/预处理后的展示流程，不能代表真实医院完整 DICOM 的稳定自动报告能力；后续交接师兄时应明确“能跑完流程”与“能产出可信报告”是两件事。


## 2026-08-13 14:03:00 +0800 Tissue LGE 显示修复与 GitHub 可回滚发布

- 任务：修复两个 lge_sax 均显示 设为 Tissue LGE的状态错误，并把自动测试、发布 tag、GitHub 推送和安全回滚纳入工作流。
- 已确认：Study 735 在无显式主标记时实际默认使用第一条 lge_sax；前端此前只识别显式 is_tissue_lge_primary，导致两条都显示为未选择。
- 已实现：默认第一条显示 Tissue LGE ✓；人工切换后唯一勾选移动，刷新/重新打开同一 Study 后保持；非 lge_sax 隐藏按钮，Overview 的 Matrix 继续隐藏。
- 已验证：apps/web typecheck、13 项前端测试、冻结运行时构建、主站 production build、22 项 CVI API 定向回归和 20 项病例访问保护测试通过；真实浏览器完成默认、切换、刷新持久化验收。
- 版本控制：源码按 3 个提交整理，创建并推送 annotated tag v2026.08.13-2；GitHub origin/main 与本机 HEAD 均为 f949472。新增 CI、发布脚本和非破坏式回滚脚本，回滚通过新 commit 完成，不重写历史。
- 边界：未读取或提交病例、DICOM、数据库、日志、密钥、zian_workspace 或 /home/Larry/data/；线上静态文件有独立备份。
- 推测：暂无。

## 2026-08-15 20:00:09 +0800 CMR 实习生反馈可复现化与 Mac 调窗兼容

- 任务：梳理实习生反馈中“排除区重算后数值不变”“排除区微调/拖拽产生大量灰色副本”“功能评估指标行不同”和 Mac 无法右键调窗四类问题，并把修复和反馈规范纳入工作站交付。
- 已确认：多排除区旧编辑链路丢失目标区域身份，点状微调、微调模式和拖拽可能把目标区域当成新排除区追加；自动保存进行中时旧重算入口可能按旧轮廓计算；功能评估旧页面按病例现有键动态展示，导致不同病例指标行数不同；旧调窗判断只接受标准右键，未识别 macOS `Control + 单击`。
- 已实现：排除区使用稳定动作身份并只更新目标区域；重算流程改为等待自动保存、保存当前快照、后端重算和最新结果回填；功能评估固定标准指标行并用“未计算”表示缺值；CMR 勾画页与功能评估页新增不含患者身份信息的“复制诊断信息”；Mac 增加 `Control + 单击拖动`调窗兼容。
- 已发布：工作站版本 `v2026.08.15-2`；相关前端测试 17 项通过，主前端生产构建通过；GitHub `origin/main` 已包含提交 `2f232c6`。
- 文档：更新 `CMR工作站医生操作说明书.md` 与医生版 Word，新增 `CMR工作站问题反馈与复现规范.md`，规定失败现场不要先刷新/换电脑、固定反馈模板、排除区附加字段、隐私裁剪和五项验收清单。
- 边界：未读取病例、DICOM、数据库、日志、密钥或患者正文；反馈截图只用于识别功能现象，未把其中身份信息写入代码、文档或工作日志。
- 待验收：真实 Mac 的 Control 点击调窗、两个排除区中只编辑第一个、保存后立即重算、剪贴板拒绝时的手动复制兜底，仍需由已登录用户在真实病例页面完成手工验收。
- 推测：暂无。
