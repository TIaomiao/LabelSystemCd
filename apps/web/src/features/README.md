# Maintained CMR feature UI

This directory is the target home for modular CMR feature interfaces. M0 establishes the boundary only; the current embedded viewer continues to run through the existing compatibility pipeline.

Feature UI must consume versioned contracts from `contracts/cmr/` and must not infer clinical acceptance from the presence of a numeric result.
