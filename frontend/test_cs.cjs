const cornerstoneTools = require('cornerstone-tools');
console.log(Object.keys(cornerstoneTools.getModule('segmentation').setters));
console.log(Object.keys(cornerstoneTools.getModule('segmentation').configuration));
